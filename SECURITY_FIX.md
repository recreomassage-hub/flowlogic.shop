# 🔒 Экстренное исправление утечки SSH ключа

## ⚠️ КРИТИЧЕСКАЯ ПРОБЛЕМА БЕЗОПАСНОСТИ

**Дата обнаружения:** 2025-12-23  
**Источник:** GitGuardian  
**Тип:** OpenSSH Private Key exposed  
**Репозиторий:** recreomassage-hub/flowlogic.shop

---

## 🚨 НЕМЕДЛЕННЫЕ ДЕЙСТВИЯ

### 1. Отзыв скомпрометированного ключа

**ВАЖНО:** Если приватный ключ попал в публичный доступ, он считается скомпрометированным и должен быть немедленно отозван.

#### Шаги:

1. **Удалите ключ из GitHub:**
   - Перейдите в GitHub Settings → SSH and GPG keys
   - Удалите все ключи, которые могли быть скомпрометированы

2. **Удалите ключ из серверов:**
   - Проверьте файлы `~/.ssh/authorized_keys` на всех серверах
   - Удалите соответствующий публичный ключ

3. **Сгенерируйте новый SSH ключ:**
   ```bash
   ssh-keygen -t ed25519 -C "your_email@example.com"
   # Сохраните в ~/.ssh/id_ed25519 (НЕ в папке проекта!)
   ```

4. **Добавьте новый ключ в GitHub:**
   ```bash
   cat ~/.ssh/id_ed25519.pub
   # Скопируйте вывод и добавьте в GitHub Settings → SSH and GPG keys
   ```

---

## 🧹 Очистка истории Git

### Вариант А: Полная очистка (если история не важна)

Если проект новый и вы готовы пожертвовать историей:

```bash
# 1. Удалите .git папку
rm -rf .git

# 2. Убедитесь, что .gitignore настроен правильно
# (уже обновлен)

# 3. Инициализируйте заново
git init
git add .
git commit -m "Fix: remove sensitive data and re-init repository"

# 4. Добавьте remote и запушьте с force
git remote add origin https://github.com/recreomassage-hub/flowlogic.shop.git
git branch -M main
git push -u origin main --force
```

**⚠️ ВНИМАНИЕ:** Это удалит всю историю коммитов!

### Вариант Б: Сохранение истории (рекомендуется)

Если история важна, используйте `git-filter-repo`:

#### Установка git-filter-repo:

```bash
# Ubuntu/Debian
sudo apt install git-filter-repo

# macOS
brew install git-filter-repo

# Или через pip
pip install git-filter-repo
```

#### Очистка истории:

```bash
# 1. Найдите имя файла с ключом (замените на реальное имя)
# Проверьте последние коммиты:
git log --all --full-history --name-only | grep -E "(id_rsa|id_ed25519|\.pem|\.key)"

# 2. Удалите файл из всей истории
git filter-repo --invert-paths --path путь/к/файлу/с/ключом

# 3. Force push (ОСТОРОЖНО: это перезапишет историю на GitHub!)
git push origin --force --all
git push origin --force --tags
```

**⚠️ ВНИМАНИЕ:** 
- Убедитесь, что все участники проекта знают о force push
- Сделайте backup репозитория перед выполнением
- Это действие необратимо

---

## 🛡️ Защита на будущее

### Обновленный .gitignore

Файл `.gitignore` уже обновлен и включает:

```
# SSH keys and certificates
*.pem
*.key
*.p12
*.pfx
*.crt
*.cer
id_rsa*
id_ed25519*
id_ecdsa*
id_dsa*
*.pub
known_hosts
authorized_keys
*.ppk
ssh_config
config
```

### Правила безопасности:

1. **НИКОГДА не храните приватные ключи в папке проекта**
   - Используйте стандартную системную папку `~/.ssh/`
   - Никогда не коммитьте файлы из `~/.ssh/`

2. **Используйте pre-commit hooks:**
   ```bash
   # Установите git-secrets или similar
   git secrets --install
   git secrets --register-aws
   ```

3. **Регулярно проверяйте репозиторий:**
   - Используйте GitGuardian или similar tools
   - Проверяйте перед каждым push

4. **Используйте секреты через переменные окружения:**
   - AWS SSM Parameter Store
   - GitHub Secrets
   - Vercel Environment Variables
   - НИКОГДА не хардкодите секреты в коде

---

## 📋 Чеклист исправления

- [ ] Отозван скомпрометированный SSH ключ из GitHub
- [ ] Удален ключ из всех серверов (authorized_keys)
- [ ] Сгенерирован новый SSH ключ
- [ ] Новый ключ добавлен в GitHub
- [ ] Файл с ключом удален из истории Git
- [ ] .gitignore обновлен
- [ ] Репозиторий проверен на наличие других секретов
- [ ] Настроены pre-commit hooks (опционально)
- [ ] Команда проинформирована о проблеме

---

## 🔍 Проверка после исправления

После выполнения всех шагов проверьте:

```bash
# 1. Убедитесь, что файл удален из истории
git log --all --full-history --name-only | grep -E "(id_rsa|id_ed25519|\.pem|\.key)"

# 2. Проверьте, что .gitignore работает
git check-ignore -v путь/к/тестовому/ключу

# 3. Проверьте текущий статус
git status
```

---

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте документацию GitGuardian
2. Обратитесь к документации git-filter-repo
3. Рассмотрите использование GitHub's secret scanning

---

**ВАЖНО:** После исправления GitGuardian может продолжать показывать уведомления в течение некоторого времени, пока не обновит свой индекс. Это нормально.

---

**Дата создания:** 2025-12-23  
**Статус:** Требуется немедленное исправление





