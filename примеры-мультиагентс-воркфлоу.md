# 🚀 Примеры мультиагентной разработки в Cursor

**Система:** OpenSpec + Beads  
**Платформа:** Cursor IDE  
**Максимум агентов:** 8 параллельных агентов

---

## ⚡ УСТАНОВКА АЛИАСОВ

### Автоматическая установка (рекомендуется)

```bash
# Запустить скрипт установки
./install-aliases.sh
```

Скрипт автоматически определит ваш shell и добавит алиасы в `.bashrc` или `.zshrc`.

### Ручная установка

Если предпочитаете установить вручную:

```bash
# Добавить в ~/.bashrc или ~/.zshrc
source "/path/to/project/.aliases"
```

Или для текущей сессии:
```bash
source .aliases
```

### Основные команды

**Beads CLI (используйте напрямую):**
- `bd ready` - показать готовые задачи
- `bd show {id}` - показать детали задачи
- `bd start {id}` - начать работу над задачей
- `bd complete {id}` - завершить задачу
- `bd discover "описание" --from {id}` - создать новую задачу из проблемы
- `bd create "название"` - создать задачу вручную

**Статус и утилиты:**
- `status-gen` → `./scripts/generate-status.sh`
- `status-read` → `cat STATUS.md`

**Полный список:** Используйте `bd --help` для всех команд Beads CLI

---

## 📋 СОДЕРЖАНИЕ

1. [Базовый workflow с одним агентом](#базовый-workflow)
2. [Мультиагентная разработка фичи](#мультиагентная-разработка-фичи)
3. [Параллельная работа над задачами](#параллельная-работа)
4. [Специализированные агенты по ролям](#специализированные-агенты)
5. [Composer для больших изменений](#composer-для-больших-изменений)
6. [Chat для быстрых вопросов](#chat-для-быстрых-вопросов)
7. [Автоматизация через Agents](#автоматизация-через-agents)
8. [Продвинутые сценарии](#продвинутые-сценарии)

---

## 🎯 БАЗОВЫЙ WORKFLOW

### Сценарий 1: Новая фича с одним агентом

**Шаг 0: Pre-work Checklist (ПЕРЕД созданием proposal)**
```bash
# 1. Проверить текущее состояние
openspec list                    # Активные изменения
openspec list --specs            # Существующие спецификации
openspec show user-dashboard     # Если есть связанная spec

# 2. Поиск существующей работы
openspec spec list --long        # Все спецификации
rg -n "Requirement:|Scenario:" openspec/specs  # Поиск по требованиям

# 3. Проверка конфликтов
openspec list                    # Ожидающие изменения
rg -n "^#|Requirement:" openspec/changes  # Изменения в процессе

# 4. Прочитать контекст проекта
cat openspec/project.md
```

**Вопросы:**
- Существует ли уже эта capability?
- Есть ли конфликтующие pending changes?
- Нужно ли модифицировать существующую spec vs создать новую?

**Шаг 1: Создать предложение (Cursor Chat)**

**Выбор Change ID:**
- Формат: `<verb>-<noun>-<noun>` (kebab-case)
- Примеры: `add-user-dashboard-redesign`, `update-payment-flow`
- Префиксы: `add-`, `update-`, `remove-`, `refactor-`, `fix-`
- Уникальность: Проверить `openspec list`, при необходимости добавить `-2`, `-3`

```
/openspec-proposal

Change: add-user-dashboard-redesign

Description: Redesign user dashboard with modern UI, better navigation, and real-time stats.

Why:
Current dashboard is outdated and doesn't provide real-time statistics.

What Changes:
- Modern card-based layout
- Real-time statistics display
- Quick action buttons
- Responsive design (mobile-first)
- Dark mode support

Impact:
- Affected specs: user-dashboard, notifications
- Affected code: src/frontend/components/Dashboard/
```

**Процесс создания proposal:**
1. Review `openspec/project.md`, `openspec list`, `openspec list --specs`
2. Choose unique verb-led `change-id` и scaffold структуру
3. Write `proposal.md` (Why, What Changes, Impact)
4. Write `tasks.md` (ordered checklist)
5. Write `design.md` (OPTIONAL - только если cross-cutting changes, новые зависимости, архитектурные решения)
6. Write spec deltas в `specs/<capability>/spec.md` с `## ADDED|MODIFIED|REMOVED Requirements` и `#### Scenario:`
7. Validate: `openspec validate <id> --strict`

**Результат:** 
- `openspec/changes/add-user-dashboard-redesign/proposal.md` (REQUIRED)
- `openspec/changes/add-user-dashboard-redesign/tasks.md` (REQUIRED)
- `openspec/changes/add-user-dashboard-redesign/design.md` (OPTIONAL)
- `openspec/changes/add-user-dashboard-redesign/specs/**/spec.md` (REQUIRED)

**ВАЖНО:** На этапе proposal НЕ пишем код! Только документы.

**Шаг 2: Валидация (Terminal)**
```bash
# Проверить валидность предложения
openspec validate add-user-dashboard-redesign --strict

# Показать детали предложения
openspec show add-user-dashboard-redesign

# Показать только deltas
openspec show add-user-dashboard-redesign --json --deltas-only
```

**Если валидация не проходит:** Исправить все ошибки перед отправкой на approval.

**Шаг 3: Ждать approval ("Go!")**
**КРИТИЧЕСКОЕ ПРАВИЛО:** 🚫 NO CODE before explicit "Go!" approval

**Шаг 4: Реализация (после approval, Cursor Composer)**
```
/openspec-apply

@openspec/changes/add-user-dashboard-redesign/proposal.md
@openspec/changes/add-user-dashboard-redesign/design.md  # если есть
@openspec/changes/add-user-dashboard-redesign/tasks.md
@openspec/changes/add-user-dashboard-redesign/specs/**/spec.md

Implement changes according to proposal and tasks.
```

**Процесс реализации:**
1. Read `proposal.md`, `design.md` (if present), и `tasks.md`
2. Work through tasks sequentially из `tasks.md`
3. Keep edits minimal и focused
4. После завершения: update checklist (`- [ ]` → `- [x]`)

**Шаг 5: Конвертировать в Beads issues (опционально, Cursor Chat)**
```
/openspec-to-beads

add-user-dashboard-redesign
```

**Результат:** Задачи в `.beads/issues.jsonl` (структурированные для Issue Tracking)

**Шаг 6: Работа через Beads (Terminal + Cursor)**
```bash
# Найти готовые задачи
bd ready

# Начать работу над задачей
bd start DASH-1
```

В Cursor Composer:
```
@openspec/changes/add-user-dashboard-redesign/tasks.md
@openspec/changes/add-user-dashboard-redesign/specs/**/spec.md

Task: DASH-1 from tasks.md
Implement according to spec and tasks.
```

**Шаг 7: Архивация (после deployment, Cursor Chat)**
```
/openspec-archive

add-user-dashboard-redesign
```

**Процесс архивации:**
1. Validate change ID через `openspec list`
2. Run `openspec archive <id> --yes` (или `--skip-specs` для tooling-only)
3. Review output
4. Validate: `openspec validate --strict`

**Результат:** 
- `openspec/changes/archive/YYYY-MM-DD-add-user-dashboard-redesign/`
- Обновленные `openspec/specs/**/spec.md` (если были deltas)

---

## 👥 МУЛЬТИАГЕНТНАЯ РАЗРАБОТКА ФИЧИ

### Сценарий 2: Параллельная работа 4 агентов

**Настройка:**
1. **Pre-work:** Проверить текущее состояние через `openspec list` и `openspec list --specs`
2. **Создать proposal:** `/openspec-proposal` (один раз) - НЕ писать код на этом этапе!
3. **Validate:** `openspec validate add-user-dashboard-redesign --strict`
4. **Дождаться approval ("Go!")** - КРИТИЧЕСКОЕ ПРАВИЛО: 🚫 NO CODE before approval
5. **Опционально:** Конвертировать в Beads issues через `/openspec-to-beads`
6. Открыть Cursor Composer (Cmd/Ctrl + I)
7. Включить режим "Agents" (до 8 агентов)
8. Настроить агентов по ролям

**Агент 1: Backend Developer**
```
Agent Role: Backend Developer
Focus: API endpoints, database queries, business logic

@openspec/changes/add-user-dashboard-redesign/proposal.md
@openspec/changes/add-user-dashboard-redesign/design.md  # если есть
@openspec/changes/add-user-dashboard-redesign/tasks.md
@openspec/changes/add-user-dashboard-redesign/specs/**/spec.md

Task: DASH-2 (Backend API for dashboard statistics) from tasks.md

Implement according to proposal and tasks:
- GET /api/v1/dashboard/stats endpoint
- Database queries for statistics
- Caching layer for performance
- Error handling and validation

Follow backend conventions from openspec/project.md.
Work through tasks sequentially from tasks.md.
```

**Агент 2: Frontend Developer**
```
Agent Role: Frontend Developer
Focus: UI components, state management, user experience

@openspec/changes/add-user-dashboard-redesign/proposal.md
@openspec/changes/add-user-dashboard-redesign/design.md  # если есть
@openspec/changes/add-user-dashboard-redesign/tasks.md
@openspec/changes/add-user-dashboard-redesign/specs/**/spec.md

Task: DASH-3 (Dashboard UI Components) from tasks.md

Implement according to proposal and tasks:
- Dashboard layout component
- Statistics cards component
- Quick actions component
- Responsive design
- Dark mode support

Use design system components from openspec/specs/design-system/.
Work through tasks sequentially from tasks.md.
```

**Агент 3: QA Engineer**
```
Agent Role: QA Engineer
Focus: Testing, quality assurance, edge cases

@openspec/changes/add-user-dashboard-redesign/proposal.md
@openspec/changes/add-user-dashboard-redesign/design.md  # если есть
@openspec/changes/add-user-dashboard-redesign/tasks.md
@openspec/changes/add-user-dashboard-redesign/specs/**/spec.md

Task: DASH-4 (Testing plan and implementation) from tasks.md

Implement according to proposal and tasks:
- Unit tests for dashboard components
- Integration tests for API endpoints
- E2E tests for user flows
- Test data fixtures
- Test coverage report

Follow testing standards from openspec/project.md.
Work through tasks sequentially from tasks.md.
```

**Агент 4: DevOps Engineer**
```
Agent Role: DevOps Engineer
Focus: Deployment, monitoring, infrastructure

@openspec/changes/add-user-dashboard-redesign/proposal.md
@openspec/changes/add-user-dashboard-redesign/design.md  # если есть
@openspec/changes/add-user-dashboard-redesign/tasks.md
@openspec/changes/add-user-dashboard-redesign/specs/**/spec.md

Task: DASH-5 (Deployment and monitoring) from tasks.md

Implement according to proposal and tasks:
- Environment variables
- CI/CD pipeline updates
- Monitoring dashboards
- Performance metrics
- Rollback procedures

Follow deployment guidelines from openspec/project.md.
Work through tasks sequentially from tasks.md.
```

**Координация:**
```bash
# Проверить статус всех задач
bd ready --json | jq '.[] | select(.epic == "add-user-dashboard-redesign")'

# Начать работу над задачами
bd start DASH-2
bd start DASH-3
bd start DASH-4
bd start DASH-5

# Обновить статус после завершения
status-gen
```

---

## 🔄 ПАРАЛЛЕЛЬНАЯ РАБОТА НАД ЗАДАЧАМИ

### Сценарий 3: 8 агентов работают параллельно

**Структура задач:**
```
Change: implement-design-system
Tasks: DS-1, DS-2, DS-3, DS-4, DS-5, DS-6, DS-7, DS-8
```

**Агент 1: Design Tokens (DS-1)**
```
@openspec/changes/implement-design-system/proposal.md
@openspec/changes/implement-design-system/tasks.md
@openspec/changes/implement-design-system/specs/**/spec.md

Task: DS-1

Update tailwind.config.js with design tokens:
- Colors (background, text, accent, state)
- Typography (font sizes, line heights)
- Spacing (2, 4, 6, 8)
- Border radius

Follow design system spec exactly.
```

**Агент 2: Button Component (DS-2)**
```
@openspec/changes/implement-design-system/proposal.md
@openspec/changes/implement-design-system/tasks.md
@openspec/changes/implement-design-system/specs/**/spec.md

Task: DS-2

Create Button component:
- Variants: primary, secondary, text
- Sizes: sm, md, lg
- States: default, disabled, loading
- Accessibility: WCAG 2.1 AA
- Tests: unit tests

Follow component spec from openspec/specs/design-system/.
```

**Агент 3: Input Component (DS-3)**
```
@openspec/changes/implement-design-system/proposal.md
@openspec/changes/implement-design-system/tasks.md
@openspec/changes/implement-design-system/specs/**/spec.md

Task: DS-3

Create Input component:
- Types: text, email, password, number
- States: default, error, disabled
- Validation: Zod integration
- Accessibility: WCAG 2.1 AA
- Tests: unit tests

Follow component spec from openspec/specs/design-system/.
```

**Агент 4: Card Component (DS-4)**
```
@openspec/changes/implement-design-system/proposal.md
@openspec/changes/implement-design-system/tasks.md
@openspec/changes/implement-design-system/specs/**/spec.md

Task: DS-4

Create Card component:
- Variants: default, elevated, outlined
- Sections: header, body, footer
- Responsive: mobile-first
- Accessibility: semantic HTML
- Tests: unit tests

Follow component spec from openspec/specs/design-system/.
```

**Агент 5: Typography Components (DS-5)**
```
@openspec/changes/implement-design-system/proposal.md
@openspec/changes/implement-design-system/tasks.md
@openspec/changes/implement-design-system/specs/**/spec.md

Task: DS-5

Create Typography components:
- Heading (h1-h6)
- Paragraph
- Label
- Caption
- Accessibility: semantic HTML
- Tests: unit tests

Follow typography spec from openspec/specs/design-system/.
```

**Агент 6: Modal Component (DS-6)**
```
@openspec/changes/implement-design-system/proposal.md
@openspec/changes/implement-design-system/tasks.md
@openspec/changes/implement-design-system/specs/**/spec.md

Task: DS-6

Create Modal component:
- Variants: default, fullscreen
- Accessibility: focus trap, ARIA attributes
- Keyboard navigation: ESC to close
- Tests: unit + integration tests

Follow modal spec from openspec/specs/design-system/.
```

**Агент 7: Documentation (DS-7)**
```
@openspec/changes/implement-design-system/proposal.md
@openspec/changes/implement-design-system/tasks.md
@openspec/changes/implement-design-system/specs/**/spec.md

Task: DS-7

Create component documentation:
- Storybook stories for each component
- Usage examples
- Props documentation
- Accessibility notes
- Migration guide

Follow documentation standards from openspec/project.md.
```

**Агент 8: Integration Tests (DS-8)**
```
@openspec/changes/implement-design-system/proposal.md
@openspec/changes/implement-design-system/tasks.md
@openspec/changes/implement-design-system/specs/**/spec.md

Task: DS-8

Create integration tests:
- Component interactions
- Accessibility tests (axe-core)
- Visual regression tests
- E2E tests for critical flows

Follow testing standards from openspec/project.md.
```

**Координация:**
```bash
# Начать все задачи параллельно
bd start DS-1 &
bd start DS-2 &
bd start DS-3 &
bd start DS-4 &
bd start DS-5 &
bd start DS-6 &
bd start DS-7 &
bd start DS-8 &

# Дождаться завершения всех задач
wait

# Обновить статус
status-gen
```

---

## 🎨 СПЕЦИАЛИЗИРОВАННЫЕ АГЕНТЫ ПО РОЛЯМ

### Сценарий 4: Backend + Frontend + QA параллельно

**Агент 1: Backend API (API-1)**
```
Agent Role: Backend Developer
Focus: REST API, database, business logic

@openspec/changes/add-payment-integration/proposal.md
@openspec/changes/add-payment-integration/tasks.md
@openspec/changes/add-payment-integration/specs/**/spec.md

Task: API-1

Implement:
- POST /api/v1/payments/create endpoint
- Payment validation (Zod schemas)
- Stripe integration
- Error handling
- Logging

Follow backend conventions from openspec/project.md.
Use TypeScript, Express.js, Zod validation.
```

**Агент 2: Frontend Form (FRONT-1)**
```
Agent Role: Frontend Developer
Focus: UI, forms, user experience

@openspec/changes/add-payment-integration/proposal.md
@openspec/changes/add-payment-integration/tasks.md
@openspec/changes/add-payment-integration/specs/**/spec.md

Task: FRONT-1

Implement:
- Payment form component
- Form validation (React Hook Form + Zod)
- Error handling
- Loading states
- Success/error feedback

Follow frontend conventions from openspec/project.md.
Use React, TypeScript, Tailwind CSS.
```

**Агент 3: QA Tests (QA-1)**
```
Agent Role: QA Engineer
Focus: Testing, quality assurance

@openspec/changes/add-payment-integration/proposal.md
@openspec/changes/add-payment-integration/specs/**/spec.md

Task: QA-1

Create:
- Unit tests for payment form
- Integration tests for API endpoint
- E2E tests for payment flow
- Test data fixtures
- Error scenario tests

Follow testing standards from openspec/project.md.
```

---

## 🎭 COMPOSER ДЛЯ БОЛЬШИХ ИЗМЕНЕНИЙ

### Сценарий 5: Большой рефакторинг с одним агентом

**Перед началом:**
1. Pre-work: Проверить `openspec list`, `openspec show user-authentication` (если spec существует)
2. Создать proposal через `/openspec-proposal` для рефакторинга
3. Validate: `openspec validate refactor-authentication-system --strict`
4. Дождаться approval ("Go!")

**Реализация:**
```
/openspec-apply

@openspec/changes/refactor-authentication-system/proposal.md
@openspec/changes/refactor-authentication-system/design.md  # должно быть для архитектурных изменений
@openspec/changes/refactor-authentication-system/tasks.md
@openspec/changes/refactor-authentication-system/specs/**/spec.md

Refactor authentication system according to proposal and tasks:
- Work through tasks sequentially from tasks.md
- Follow architecture decisions from design.md
- Keep changes minimal and focused
- Update checklist after each completed task

Implement tasks sequentially from tasks.md.
```

---

## 💬 CHAT ДЛЯ БЫСТРЫХ ВОПРОСОВ

### Сценарий 6: Быстрые вопросы и разъяснения

**Вопрос о существующем коде:**
```
@src/backend/services/auth.ts

Как работает текущая аутентификация? Какие улучшения можно внести?
```

**Вопрос о спецификации:**
```
@openspec/specs/user-authentication/spec.md

Нужно ли добавлять refresh token rotation? Какие требования безопасности?
```

**Вопрос о контексте проекта:**
```
@openspec/project.md

Какие библиотеки разрешены для валидации форм? Можно ли использовать Yup?
```

---

## 🤖 АВТОМАТИЗАЦИЯ ЧЕРЕЗ AGENTS

### Сценарий 7: Автоматический workflow

**Шаг 1: Создать proposal (Agents)**
```
Agent 1 (Analyst):
/openspec-proposal

Change: add-email-notifications

Analyze requirements and create proposal with:
- Why: Users need email notifications
- What Changes: Email service integration
- Impact: notifications spec, email service

Review openspec/specs/notifications/spec.md first.
```

**Шаг 2: Валидация (Agents)**
```
Agent 2 (Architect):
openspec validate add-email-notifications --strict

Review proposal and fix any validation issues.
Check for conflicts with existing changes.
```

**Шаг 3: Реализация (Agents)**
```
Agent 3 (Backend Developer):
/openspec-apply

@openspec/changes/add-email-notifications/proposal.md
@openspec/changes/add-email-notifications/tasks.md
@openspec/changes/add-email-notifications/specs/**/spec.md

Implement backend email service according to tasks.md.
```

**Шаг 5: Тестирование (Agents)**
```
Agent 4 (QA Engineer):
@openspec/changes/add-email-notifications/proposal.md
@openspec/changes/add-email-notifications/tasks.md
@openspec/changes/add-email-notifications/specs/**/spec.md

Task from tasks.md: Add comprehensive tests

Implement according to proposal and tasks:
- Unit tests for email service
- Integration tests for notification flow
- E2E tests for user scenarios

Work through tasks sequentially from tasks.md.
```

**Шаг 6: Документация (Agents)**
```
Agent 5 (Documentation):
@openspec/changes/add-email-notifications/proposal.md
@openspec/changes/add-email-notifications/tasks.md

Task from tasks.md: Update documentation

Update API documentation and user guide with email notification feature.
Work through tasks sequentially from tasks.md.
```

**Шаг 7: Архивация (после deployment)**
```
Agent 6 (DevOps):
/openspec-archive

add-email-notifications

# Процесс:
# 1. Validate: openspec list (проверить что change существует)
# 2. Archive: openspec archive add-email-notifications --yes
# 3. Validate: openspec validate --strict
```

---

## 🚀 ПРОДВИНУТЫЕ СЦЕНАРИИ

### Сценарий 8: Микросервисная архитектура

**Pre-work для обоих сервисов:**
```bash
# Проверить существующие сервисы и архитектуру
openspec list
openspec list --specs
openspec show microservices  # если есть spec о микросервисах
```

**Backend Service 1:**
```
Agent 1 (Backend):
@openspec/changes/add-user-service/proposal.md
@openspec/changes/add-user-service/design.md  # если есть
@openspec/changes/add-user-service/tasks.md
@openspec/changes/add-user-service/specs/**/spec.md

Task: USERS-1 from tasks.md

Implement user service according to proposal and tasks:
- REST API endpoints
- Database schema
- Business logic
- Error handling

Work through tasks sequentially from tasks.md.
```

**Backend Service 2:**
```
Agent 2 (Backend):
@openspec/changes/add-notification-service/proposal.md
@openspec/changes/add-notification-service/design.md  # если есть
@openspec/changes/add-notification-service/tasks.md
@openspec/changes/add-notification-service/specs/**/spec.md

Task: NOTIF-1 from tasks.md

Implement notification service according to proposal and tasks:
- Message queue integration
- Email/SMS providers
- Retry logic
- Error handling

Work through tasks sequentially from tasks.md.
```

**Frontend Integration:**
```
Agent 3 (Frontend):
@openspec/changes/add-user-service/proposal.md
@openspec/changes/add-notification-service/proposal.md

Integrate both services in frontend:
- API clients
- State management
- Error handling
- Loading states
```

### Сценарий 9: Миграция данных

**Перед началом:**
1. Pre-work: Проверить текущую схему БД, существующие миграции
2. Создать proposal через `/openspec-proposal` (обязательно с design.md для миграций!)
3. Validate: `openspec validate migrate-database-schema --strict`
4. Дождаться approval ("Go!")

**Реализация:**
```
/openspec-apply

@openspec/changes/migrate-database-schema/proposal.md
@openspec/changes/migrate-database-schema/design.md  # ОБЯЗАТЕЛЬНО для миграций
@openspec/changes/migrate-database-schema/tasks.md
@openspec/changes/migrate-database-schema/specs/**/spec.md

Migrate database schema according to proposal and tasks:
- Work through tasks sequentially from tasks.md
- Follow migration plan from design.md (rollback procedures, testing strategy)
- Implement step by step
- Update checklist after each completed task

Follow migration plan from design.md.
Implement step by step according to tasks.md.
```

**После deployment:**
```
/openspec-archive migrate-database-schema
```

---

## 📚 ПОЛЕЗНЫЕ КОМАНДЫ

### OpenSpec CLI
```bash
# Показать все активные изменения
openspec list

# Показать все спецификации
openspec list --specs

# Показать детали изменения
openspec show add-user-dashboard-redesign

# Валидация изменения
openspec validate add-user-dashboard-redesign --strict

# Архивирование после deployment
openspec archive add-user-dashboard-redesign --yes
```

### Beads CLI
```bash
# Показать готовые задачи
bd ready

# Показать детали задачи
bd show DASH-1

# Начать работу
bd start DASH-1

# Завершить задачу
bd complete DASH-1

# Обновить статус
./scripts/generate-status.sh
```

### Комбинации
```bash
# Быстрый старт работы
NEXT_ID=$(bd ready --json | jq -r '.[0].id // empty') && bd start $NEXT_ID && status-gen

# Завершение с коммитом
bd complete {issue-id} && status-gen && git add . && git commit -m "feat: {issue-id}"

# Проверка статуса проекта
status-read && bd ready
```

---

## 🎯 КЛЮЧЕВЫЕ ПРАВИЛА

### OpenSpec Workflow

1. ✅ **Pre-work перед proposal:** Всегда проверяй `openspec list`, `openspec list --specs`, и контекст проекта
2. ✅ **Создавай proposal** через `/openspec-proposal` для новых фич, breaking changes, архитектурных изменений
3. ✅ **НЕ пиши код на этапе proposal!** Только документы (proposal.md, tasks.md, design.md, spec deltas)
4. ✅ **Валидируй proposal** через `openspec validate <id> --strict` - исправляй все ошибки перед approval
5. ✅ **Жди явного approval ("Go!")** - 🚫 NO CODE before explicit approval
6. ✅ **Реализуй после approval** через `/openspec-apply` - работай последовательно по tasks.md
7. ✅ **Обновляй checklist** после завершения: `- [ ]` → `- [x]` в tasks.md
8. ✅ **Конвертируй в Beads** опционально через `/openspec-to-beads` для структурированной работы
9. ✅ **Архивируй после deployment** через `/openspec-archive` с `--yes` флагом

### Beads Issue Tracking

10. ✅ **Одна задача на агента** (избегай перегрузки)
11. ✅ **Обновляй статус** перед завершением сессии через `status-gen`
12. ✅ **Коммить .beads/** вместе с кодом
13. ✅ **Работай только с ready задачами** - используй `bd ready` для поиска

### Decision Tree

**Создавать proposal?**
- ✅ Новая фича/функциональность → CREATE PROPOSAL
- ✅ Breaking changes → CREATE PROPOSAL
- ✅ Архитектурные изменения → CREATE PROPOSAL
- ❌ Bug fix (restore intended behavior) → Fix directly
- ❌ Typo/format/comment → Fix directly
- ❌ Dependency update (non-breaking) → Fix directly
- ❌ Configuration change → Fix directly

---

## 📖 ДОПОЛНИТЕЛЬНЫЕ РЕСУРСЫ

- **`openspec/AGENTS.md`** - Полные инструкции для AI агентов по OpenSpec
- **`openspec/project.md`** - Контекст проекта и технические стандарты
- **`CHEATSHEET.md`** - Быстрая справка по командам
- **`CONTRIBUTING.md`** - Руководство по вкладу в проект
- **`STATUS.md`** - Текущий статус проекта (генерируется автоматически)

---

**Счастливой разработки с OpenSpec + Beads! 🚀**
