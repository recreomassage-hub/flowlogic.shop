# 📜 **СКРИПТЫ LLM-OS И СЦЕНАРИИ ИСПОЛЬЗОВАНИЯ**

## 🛠️ **ОБЗОР ВСЕХ СКРИПТОВ**

### **Основные скрипты управления:**

```
📂 llm-os-project/
├── 🚀 setup_workflow.sh      # Инициализация системы (один раз)
├── 💾 step.sh                # Атомарный коммит (после каждой задачи)
├── 📊 monitor.sh             # Мониторинг в реальном времени
├── 🔧 create_scripts.sh      # Создает утилиты
├── 📈 generate_report.sh     # Генерация отчетов
└── 🎮 llmos                  # Основная утилита (обертка)
```

### **Вспомогательные скрипты:**
```
📂 .cursor/rules/             # Правила для Cursor IDE
📂 .templates/               # Шаблоны для быстрого создания
📂 reports/                  # Автоматические отчеты
```

### **Процессные сценарии (NEW):**
```
📂 scripts/
├── 🔍 quality_gate.sh           # Аудит качества этапа
├── 🔄 rollback_stage.sh         # Контролируемый откат
├── 🔄 role_reset.sh             # Перезапуск роли
├── 🧯 detect_fake_progress.sh   # Детектор фейк-прогресса
├── 🧨 mvp_mode.sh               # Overengineering Kill Switch
├── 🤝 parallel_roles.sh         # Параллельные роли
└── 🧠 knowledge_freeze.sh       # Сохранение знаний
```

## 🎯 **КАК ИСПОЛЬЗОВАТЬ КАЖДЫЙ СКРИПТ**

### **1. `setup_workflow.sh` - Инициализация системы**
**Когда использовать:** ОДИН раз в начале проекта

```bash
# Запуск:
./setup_workflow.sh

# Что делает:
✅ Создает всю структуру файлов
✅ Инициализирует Git репозиторий  
✅ Создает .gitignore
✅ Генерирует базовые файлы
```

### **2. `step.sh` - Атомарный коммит**
**Когда использовать:** ПОСЛЕ КАЖДОЙ выполненной задачи

```bash
# Запуск:
./step.sh

# Что делает:
✅ Автоматически определяет роль и этап
✅ Обновляет дату в WORKFLOW_STATE.md
✅ Создает осмысленное сообщение коммита
✅ Делает git add + commit
✅ Показывает последние коммиты
```

### **3. `monitor.sh` - Мониторинг системы**
**Когда использовать:** ВО ВРЕМЯ работы ИИ-агентов

```bash
# Запуск в отдельном терминале:
./monitor.sh

# Что показывает каждые 10 сек:
📊 Текущая роль и этап
📈 Прогресс выполнения
❓ Открытые вопросы
📝 История коммитов
```

### **4. `llmos` - Основная утилита**
**Когда использовать:** ДЛЯ БЫСТРЫХ КОМАНД

```bash
# Проверка статуса:
./llmos status

# Коммит:
./llmos commit

# Мониторинг:
./llmos monitor

# Инициализация:
./llmos start
```

## 🎬 **РЕАЛЬНЫЕ СЦЕНАРИИ ИСПОЛЬЗОВАНИЯ**

### **СЦЕНАРИЙ 1: Начало нового проекта**
**Цель:** Создать проект "TaskFlow" - систему управления задачами с ИИ

```bash
# Терминал 1: Инициализация
cd ~/projects
mkdir taskflow && cd taskflow
./setup_workflow.sh
git add . && git commit -m "🚀 Проект TaskFlow: Инициализация LLM-OS"

# Терминал 2: Мониторинг (оставляем открытым)
./monitor.sh

# Cursor: Запуск ANALYST
# Промпт: "Активируй LLM-OS. Прочитай контекст. Создай PRD для TaskFlow - системы управления задачами с ИИ-помощником."

# Терминал 3: Коммиты каждые 30 минут
while true; do
    sleep 1800  # 30 минут
    ./step.sh
done
```

### **СЦЕНАРИЙ 2: Ежедневная работа над этапом**
**Цель:** Разработать архитектуру для "TaskFlow"

```bash
# Утро: Проверка состояния
./llmos status
# Вывод: current_role: ARCHITECT, current_stage: architecture

# Cursor: Промпт для ARCHITECT
"ARCHITECT, продолжай работу над архитектурой TaskFlow. 
Сегодня нужно: 
1. Завершить C4 диаграммы контейнеров
2. Спроектировать API для задач и пользователей
3. Определить технологии для real-time уведомлений"

# Во время работы: Мониторинг (отдельный терминал)
./monitor.sh

# После каждого выполненного раздела:
./step.sh
# Автоматически: [ARCHITECT] architecture (2/5) @14:30

# В конце дня: Финал
./step.sh
./llmos status
```

### **СЦЕНАРИЙ 3: Обработка блокировки**
**Цель:** ARCHITECT обнаружил противоречие в требованиях

```bash
# Cursor показывает: 
"⚠️ ARCHITECT: Обнаружено противоречие. 
В PRD сказано 'real-time уведомления', но нет требований к latency.
Статус: BLOCKED. Смотри open_questions."

# Проверяем open_questions:
./llmos status
# Видим: open_questions: Q1 - Какая максимальная задержка для real-time?

# OWNER (вы) отвечает:
# 1. Открываем WORKFLOW_STATE.md
# 2. Находим вопрос Q1
# 3. Отвечаем в том же файле:
#    answer: "Макс задержка 500мс для 95% уведомлений"
#    answered_by: OWNER
#    answered_at: 2024-01-15T15:30:00Z

# 4. Меняем статус:
sed -i 's/статус: `BLOCKED`/статус: `IN_PROGRESS`/' WORKFLOW_STATE.md

# 5. В Cursor: "ARCHITECT, вопрос решен. Продолжай работу."
```

### **СЦЕНАРИЙ 4: Передача этапа между ролями**
**Цель:** ANALYST завершил требования, передаем ARCHITECT

```bash
# ANALYST закончил:
# WORKFLOW_STATE.md показывает:
# status: DONE, next_role: ARCHITECT, выполнено: 5/5

# OWNER проверяет артефакты:
ls docs/requirements/
# PRD.md, user_stories.md, glossary.md, qna.md - все есть

# OWNER обновляет состояние:
sed -i 's/current_role: ANALYST/current_role: ARCHITECT/' WORKFLOW_STATE.md
sed -i 's/current_stage: requirements/current_stage: architecture/' WORKFLOW_STATE.md
sed -i 's/status: DONE/status: IN_PROGRESS/' WORKFLOW_STATE.md

# Коммит изменений:
./step.sh

# Cursor для ARCHITECT:
"ARCHITECT, этап requirements завершен. 
Начни проектирование архитектуры на основе docs/requirements/PRD.md"
```

### **СЦЕНАРИЙ 5: Еженедельный отчет**
**Цель:** Сгенерировать отчет о прогрессе за неделю

```bash
# Пятница, 17:00
./generate_report.sh > reports/weekly_2024_01_19.md

# Смотрим отчет:
cat reports/weekly_2024_01_19.md

# Пример вывода:
📊 Weekly Report: 2024-01-15 to 2024-01-19
========================================
✅ Завершено этапов: 2/8
📈 Общий прогресс: 25%
⏱️ Среднее время на этап: 6 часов
❓ Открытых вопросов: 3
🔥 Самая активная роль: ANALYST (12 коммитов)
🐌 Самый долгий этап: architecture (8 часов)

# Отправляем отчет (если настроено):
# git add reports/
# git commit -m "📊 Weekly report 2024-01-19"
# git push
```

## 🔄 **РАБОЧИЙ ДЕНЬ С LLM-OS**

### **9:00 - Старт дня**
```bash
# Открываем 3 терминала:
# Т1: Работа с Cursor
# Т2: ./monitor.sh
# Т3: Для команд

./llmos status
# Проверяем где остановились
```

### **9:15 - Начало работы с ИИ**
```bash
# Копируем промпт из WORKFLOW_STATE.md
# Или используем стандартный:
"Продолжай работу над этапом [current_stage]. 
Сегодняшние задачи: [из ROLES/файла]"
```

### **11:00 - Утренний коммит**
```bash
./step.sh
# Автоматически: [ARCHITECT] architecture (3/5) @11:00
```

### **13:00 - Обеденный перерыв**
```bash
# Фиксируем прогресс
./step.sh

# Проверяем open_questions
grep -A5 "open_questions:" WORKFLOW_STATE.md
```

### **15:00 - Послеобеденная сессия**
```bash
# Если есть вопросы от ИИ - отвечаем
# Иначе продолжаем работу

./monitor.sh  # Проверяем прогресс
```

### **17:00 - Завершение дня**
```bash
# Финальный коммит
./step.sh

# Генерация дневного отчета
./generate_report.sh > reports/daily_$(date +%Y%m%d).md

# Планирование на завтра
echo "Завтра: $(grep 'next_role:' WORKFLOW_STATE.md)"
```

## 🚨 **СЦЕНАРИИ ОШИБОК И ИХ РЕШЕНИЕ**

### **Проблема 1: ИИ "забыл" контекст**
```bash
# Симптом: ИИ начинает вежливости "Как ИИ-ассистент..."
# Решение:
"Прочитай PROJECT_CONFIG.md и WORKFLOW_STATE.md.
Твоя роль: $(grep 'current_role:' WORKFLOW_STATE.md).
Продолжай с того места где остановился."
```

### **Проблема 2: Нет прогресса в WORKFLOW_STATE.md**
```bash
# Симптом: ИИ работает, но не обновляет чекбоксы
# Решение:
"После выполнения задачи обнови WORKFLOW_STATE.md:
- Отметь чекбокс: [ ] → [x] задача @timestamp
- Обнови счетчик: выполнено: X/Y
Затем запусти ./step.sh"
```

### **Проблема 3: Конфликт в Git**
```bash
# Симптом: step.sh не работает, git конфликты
# Решение:
# 1. Проверяем статус
git status

# 2. Если конфликты:
git mergetool
# ИЛИ
git checkout -- .  # Осторожно! Откатывает изменения

# 3. Вручную обновляем WORKFLOW_STATE.md
# 4. Коммит:
git add .
git commit -m "[FIX] Разрешил конфликт"
```

### **Проблема 4: ИИ работает не по роли**
```bash
# Симптом: BACKEND_DEV пытается менять архитектуру
# Решение:
"Стоп! Твоя роль: BACKEND_DEV (см WORKFLOW_STATE.md).
Ты можешь работать только с src/backend/.
Верни изменения вне твоей зоны и продолжай по инструкциям из ROLES/04_backend_dev.md"
```

## 🎮 **БЫСТРЫЕ КОМАНДЫ ДЛЯ ПАМЯТИ**

```bash
# Шпаргалка на рабочий стол:
cat > cheatsheet.sh << 'EOF'
#!/bin/bash
echo "🎮 LLM-OS CHEATSHEET"
echo "==================="
echo ""
echo "📁 Структура:"
echo "  .cursorrules     - Правила ИИ"
echo "  PROJECT_CONFIG.md - ROM системы"  
echo "  WORKFLOW_STATE.md - RAM системы"
echo "  ROLES/           - Инструкции"
echo "  docs/            - Артефакты"
echo ""
echo "🛠️ Скрипты:"
echo "  ./setup_workflow.sh  - Один раз в начале"
echo "  ./step.sh           - После каждой задачи"
echo "  ./monitor.sh        - В отдельном терминале"
echo "  ./llmos status      - Быстрая проверка"
echo ""
echo "🔄 Рабочий процесс:"
echo "  1. ИИ читает WORKFLOW_STATE.md"
echo "  2. Определяет роль"
echo "  3. Работает по ROLES/файлу"
echo "  4. Обновляет WORKFLOW_STATE.md"
echo "  5. ./step.sh"
echo ""
echo "🚨 При блокировке:"
echo "  1. Проверить open_questions"
echo "  2. Ответить как OWNER"
echo "  3. Сменить статус на IN_PROGRESS"
echo "  4. Продолжить работу"
EOF

chmod +x cheatsheet.sh
./cheatsheet.sh
```

## 📊 **МЕТРИКИ УСПЕХА**

### **По скриптам:**
- `step.sh` запускается 3-5 раз в день ✅
- `monitor.sh` показывает актуальные данные ✅  
- Отчеты генерируются автоматически ✅
- Конфликтов в Git < 1 в неделю ✅

### **По процессу:**
- Этап завершается за 2-8 часов ✅
- Между этапами < 30 минут простоя ✅
- Open questions решаются < 2 часов ✅
- Прогресс виден в реальном времени ✅

## 🎯 **ИДЕАЛЬНЫЙ РАБОЧИЙ ДЕНЬ:**

```
08:45 🏁 Старт: ./monitor.sh
09:00 🤖 ИИ начинает: Читает контекст, определяет задачи
10:30 💾 Коммит 1: ./step.sh 
12:00 🍽️ Обед: Фиксируем прогресс
13:30 🤖 ИИ продолжает: Следующие задачи
15:00 💾 Коммит 2: ./step.sh
16:30 📊 Отчет: ./generate_report.sh
17:00 🏁 Финиш: Финальный коммит, план на завтра
```

**Система готова!** Теперь у тебя есть полный набор скриптов и четкие сценарии работы. Начни с Сценария 1 и наблюдай, как LLM-OS превращает хаотичное общение с ИИ в детерминированный производственный процесс! 🚀


## 🚀 **Реализация скриптов LLM-OS (готовые файлы)**

Отличный план! Вот **полная реализация всех скриптов** — скопируй и запусти **прямо сейчас**:

## 1. **`setup_workflow.sh` - Инициализация (один раз)**

bash

`cat > setup_workflow.sh << 'EOF' #!/bin/bash set -e echo "🚀 LLM-OS: Инициализация системы..." # Создать структуру mkdir -p ROLES docs/{requirements,architecture,planning,backend,frontend,infra,qa,docs,security} .github/workflows reports .templates .cursor/rules # Создать роли roles=("01_analyst" "02_architect" "03_pm" "04_backend_dev" "05_frontend_dev" "06_infra_devops" "07_qa" "08_docs" "09_security") for role in "${roles[@]}"; do touch "ROLES/$role.md"; done # Создать .gitignore cat > .gitignore << 'IGN' .env* !.env.example node_modules/ logs/ reports/*.log EOF # Git init git init 2>/dev/null || true git add .gitignore git commit -m "🚀 [SYSTEM] LLM-OS initialized" || true echo "✅ Система готова! Запусти: ./llmos status" EOF chmod +x setup_workflow.sh`

## 2. **`step.sh` - Атомарный коммит**

bash

`cat > step.sh << 'EOF' #!/bin/bash set -e # Определяем роль из WORKFLOW_STATE.md ROLE=$(grep -A1 "current_role:" WORKFLOW_STATE.md 2>/dev/null | tail -1 | tr -d ' ' | sed 's/.*://') STAGE=$(grep -A1 "current_stage:" WORKFLOW_STATE.md 2>/dev/null | tail -1 | tr -d ' ' | sed 's/.*://') # Обновляем дату sed -i "s/last_update:.*/last_update: $(date -u +%Y-%m-%dT%H:%M:%SZ)/" WORKFLOW_STATE.md 2>/dev/null || true # Git step git add . git diff --cached --stat git commit -m "💾 [$ROLE] $STAGE ($(git status --porcelain | wc -l | xargs) files) @$(date +%H:%M)" || echo "ℹ️ Нет изменений" # Показываем статус echo "📊 Статус:" grep -E "(current_role|current_stage|status):" WORKFLOW_STATE.md 2>/dev/null || echo "WORKFLOW_STATE.md не найден" echo "" git log --oneline -5 EOF chmod +x step.sh`

## 3. **`monitor.sh` - Реал-тайм мониторинг**

bash

`cat > monitor.sh << 'EOF' #!/bin/bash while true; do     clear    echo "📊 LLM-OS MONITOR (обновление каждые 10 сек)"    echo "====================================="         if [ -f WORKFLOW_STATE.md ]; then        echo "🎯 Текущая роль: $(grep -A1 'current_role:' WORKFLOW_STATE.md | tail -1)"        echo "📋 Этап: $(grep -A1 'current_stage:' WORKFLOW_STATE.md | tail -1)"        echo "⏰ Последнее обновление: $(grep -A1 'last_update:' WORKFLOW_STATE.md | tail -1)"        echo ""        grep -A10 "$(grep 'current_stage:' WORKFLOW_STATE.md | head -1)" WORKFLOW_STATE.md | head -15    else        echo "⚠️ WORKFLOW_STATE.md не найден"    fi         echo ""    echo "💾 Последние коммиты:"    git log --oneline -3 2>/dev/null || echo "Git не инициализирован"         echo ""    echo "Файлы изменены: $(git status --porcelain | wc -l)"    sleep 10 done EOF chmod +x monitor.sh`

## 4. **`llmos` - Основная утилита**

bash

`cat > llmos << 'EOF' #!/bin/bash case "$1" in     status)        echo "📊 LLM-OS Status:"        grep -E "(current_role|current_stage|status|next_role):" WORKFLOW_STATE.md 2>/dev/null || echo "WORKFLOW_STATE.md не найден"        echo ""        git status --short        ;;    commit)        ./step.sh        ;;    monitor)        ./monitor.sh        ;;    start)        ./setup_workflow.sh        ;;    report)        ./generate_report.sh        ;;    *)        echo "🎮 LLM-OS Commands:"        echo "  ./llmos status    - Показать статус"        echo "  ./llmos commit    - Коммит + обновить состояние"        echo "  ./llmos monitor   - Мониторинг в реальном времени"        echo "  ./llmos start     - Инициализация"        echo "  ./llmos report    - Генерация отчета"        ;; esac EOF chmod +x llmos`

## 5. **`generate_report.sh` - Отчеты**

bash

``cat > generate_report.sh << 'EOF' #!/bin/bash REPORT_DATE=$(date +%Y%m%d) cat > "reports/daily_$REPORT_DATE.md" << REPORT # 📊 LLM-OS Daily Report: $(date +%Y-%m-%d) ## 📈 Прогресс $(grep -A20 "$(grep 'current_stage:' WORKFLOW_STATE.md | head -1)" WORKFLOW_STATE.md 2>/dev/null | grep -E "\[.\]|\[ \]" | wc -l | xargs) / 8 задач выполнено ## 🎯 Текущее состояние \`\`\` $(grep -E "(current_role|current_stage|status):" WORKFLOW_STATE.md 2>/dev/null) \`\`\` ## 💾 Коммиты за день $(git log --since="1 day ago" --oneline 2>/dev/null || echo "Нет коммитов") ## 📝 Open Questions $(grep -A10 "open_questions:" WORKFLOW_STATE.md 2>/dev/null || echo "Нет открытых вопросов") REPORT echo "✅ Отчет сохранен: reports/daily_$REPORT_DATE.md" cat "reports/daily_$REPORT_DATE.md" EOF chmod +x generate_report.sh``

## 🎬 **ЗАПУСК СИСТЕМЫ (3 команды)**

bash

`cd ~/Obsidian\ Vault/llm-os-project # 1. Инициализация ./llmos start # 2. Мониторинг (новый терминал) ./llmos monitor # 3. Проверка ./llmos status`

## 🚀 **ПЕРВЫЙ РАБОЧИЙ ЦИКЛ**

text

`Терминал 1: ./llmos monitor     # Мониторинг Терминал 2:                     # Cursor + работа Cursor> "🤖 LLM-OS активирован. Роль: ANALYST. Начинай!" 30 минут работы... Терминал 2: ./llmos commit      # Коммит`

**Система полностью автономна!** Скрипты создают **реальный production workflow** с мониторингом, отчетами и атомарными коммитами.
---

## 🧠 ПРОЦЕССНЫЕ СЦЕНАРИИ (NEW - 2025-12-26)

### **6. `quality_gate.sh` - Аудит качества этапа**
**Когда использовать:** Перед передачей этапа следующей роли

```bash
./scripts/quality_gate.sh [stage_name]
```

**Что делает:**
- Проверяет артефакты этапа
- Создает отчет для Quality Auditor
- Блокирует передачу этапа при FAIL

**См. также:** `docs/process_scenarios.md` - Сценарий 6

---

### **7. `rollback_stage.sh` - Контролируемый откат**
**Когда использовать:** Когда понимаем, что этап был ошибочным

```bash
# Найти commit
git log --oneline
./scripts/rollback_stage.sh <commit_hash> "Причина отката"
```

**Что делает:**
- Создает backup текущего состояния
- Генерирует инструкцию для отката
- Обновляет WORKFLOW_STATE.md (status: REWORK)

**См. также:** `docs/process_scenarios.md` - Сценарий 7

---

### **8. `role_reset.sh` - Перезапуск роли**
**Когда использовать:** ИИ "поплыл", генерирует воду/повторения

```bash
./scripts/role_reset.sh [role_name]
```

**Что делает:**
- Создает инструкцию для Cursor
- Очищает контекст предыдущих сообщений
- Фокусирует на текущем этапе

**См. также:** `docs/process_scenarios.md` - Сценарий 8

---

### **9. `detect_fake_progress.sh` - Детектор фейк-прогресса**
**Когда использовать:** Много коммитов, но прогресс не меняется

```bash
./scripts/detect_fake_progress.sh
```

**Что делает:**
- Анализирует коммиты и прогресс
- Проверяет наличие артефактов
- Генерирует отчет с инструкциями

**См. также:** `docs/process_scenarios.md` - Сценарий 11

---

### **10. `mvp_mode.sh` - Overengineering Kill Switch**
**Когда использовать:** ИИ начинает Event Sourcing/CQRS/Kubernetes для MVP

```bash
./scripts/mvp_mode.sh on   # Включить ограничения
./scripts/mvp_mode.sh off   # Выключить ограничения
```

**Что делает:**
- Добавляет ограничения в PROJECT_CONFIG.md
- Создает инструкцию для перепроектирования
- Ограничивает сложность решения

**См. также:** `docs/process_scenarios.md` - Сценарий 12

---

### **11. `parallel_roles.sh` - Параллельные роли**
**Когда использовать:** Нужно ускориться (BACKEND + FRONTEND)

```bash
./scripts/parallel_roles.sh BACKEND_DEV FRONTEND_DEV src/backend src/frontend
```

**Что делает:**
- Создает отдельные WORKFLOW_STATE для каждой роли
- Определяет scope для каждой роли
- Предотвращает конфликты

**См. также:** `docs/process_scenarios.md` - Сценарий 9

---

### **12. `knowledge_freeze.sh` - Сохранение знаний**
**Когда использовать:** Проект замораживается или передается

```bash
./scripts/knowledge_freeze.sh
```

**Что делает:**
- Создает knowledge dump (decisions, assumptions, risks, why_not)
- Сохраняет ключевые документы
- Превращает LLM-OS в корпоративную память

**См. также:** `docs/process_scenarios.md` - Сценарий 14

---

## 📚 ДОПОЛНИТЕЛЬНАЯ ДОКУМЕНТАЦИЯ

- **Процессные сценарии:** `docs/process_scenarios.md` - Полное описание всех сценариев
- **Quality Auditor:** `ROLES/00_quality_auditor.md` - Роль для аудита качества

---

**Последнее обновление:** 2025-12-26

---

## 🚀 FEATURE WORKFLOW (NEW - 2025-12-26)

### **13. `feature_new.sh` - Создание новой фичи**
**Когда использовать:** Перед началом разработки любой фичи

```bash
./llmos feature new <feature_name> [description]
# или
./scripts/feature_new.sh <feature_name> [description]
```

**Что делает:**
- Создает структуру фичи (brief, impact, design, qa, retro)
- Генерирует шаблоны для всех этапов
- Создает feature flag slug

**📌 Без Feature Brief фича не стартует!**

**См. также:** `docs/features/feature_workflow.md` - Полное описание

---

### **14. `feature_impact.sh` - Анализ влияния фичи**
**Когда использовать:** После Feature Brief, перед Design

```bash
./llmos feature impact <feature_slug>
# или
./scripts/feature_impact.sh <feature_slug>
```

**Что делает:**
- Создает инструкцию для ARCHITECT + PM
- Анализирует влияние на Backend, Frontend, Data, Performance, Security
- Если impact HIGH → фича должна быть разбита

**См. также:** `docs/features/feature_workflow.md` - Сценарий 16

---

### **15. `feature_check_flag.sh` - Проверка feature flag**
**Когда использовать:** Перед release, после implementation

```bash
./llmos feature check-flag <feature_slug>
# или
./scripts/feature_check_flag.sh <feature_slug>
```

**Что делает:**
- Проверяет наличие feature flag в конфигурации
- Проверяет использование в коде
- Предупреждает, если флаг не найден

**См. также:** `docs/features/feature_workflow.md` - Сценарий 20

---

### **16. `feature_retro.sh` - Ретроспектива фичи**
**Когда использовать:** После release и мониторинга (24-48 часов)

```bash
./llmos feature retro <feature_slug>
# или
./scripts/feature_retro.sh <feature_slug>
```

**Что делает:**
- Создает инструкцию для OWNER + LLM
- Анализирует что сработало / не сработало
- Генерирует выводы для следующих фич

**📌 Это обучает следующие фичи, а не только эту**

**См. также:** `docs/features/feature_workflow.md` - Сценарий 22

---

## 🧩 FEATURE-FLOW ШПАРГАЛКА

```
1. Feature brief          (PM)      → ./llmos feature new
2. Impact analysis        (ARCH)    → ./llmos feature impact
3. Minimal design         (DEV)     → Design templates
4. Micro-implementation   (DEV)     → step.sh × N
5. QA before release      (QA)      → QA template
6. Feature flag + release (PM)      → ./llmos feature check-flag
7. Rollback ready         (PM)      → Feature flag off
8. Retro                  (OWNER)   → ./llmos feature retro
```

---

## 📚 ДОПОЛНИТЕЛЬНАЯ ДОКУМЕНТАЦИЯ

- **Feature Workflow:** `docs/features/feature_workflow.md` - Полное описание процесса
- **Процессные сценарии:** `docs/process_scenarios.md` - Другие сценарии

---

**Последнее обновление:** 2025-12-26
