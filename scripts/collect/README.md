# 🔍 Collect Scripts - Сбор полного контекста

## Назначение

Скрипты для сбора полного релевантного контекста участка системы в один `.md` файл. Это решает проблему "вайб-кодинга" и туннельного зрения, когда LLM видит только часть кода и "забывает" важные связи.

## Принцип работы

Вместо разрезания кода по 100 строк (как делают Cursor/Windsurf), скрипты собирают:
- Весь релевантный код
- Схемы БД
- API спецификации
- Конфигурации
- Тесты
- Документацию

В один цельный `.md` файл, который LLM читает как единый блок.

## Доступные скрипты

### 1. `backend-auth.sh`
Сбор контекста для backend authentication:
```bash
./scripts/collect/backend-auth.sh > artifacts/collected_backend_auth.md
```

Собирает:
- API спецификацию (auth endpoints)
- Схему БД (Users/Auth)
- Routes, Controllers, Middleware
- User Model
- Cognito конфигурацию
- Тесты
- Security документацию

### 2. `frontend-ui.sh`
Сбор контекста для frontend UI:
```bash
./scripts/collect/frontend-ui.sh > artifacts/collected_frontend_ui.md
```

Собирает:
- C4 диаграммы (Frontend)
- Tech Stack
- App.tsx, Pages, Components
- State Management
- API Client
- Конфигурацию (package.json, tsconfig.json, tailwind.config.js)
- Стили
- Документацию

### 3. `smart.sh`
Умный поиск по ключевым словам:
```bash
./scripts/collect/smart.sh keyword1 keyword2 > artifacts/collected_keyword1_keyword2.md
```

Ищет по всему проекту:
- Код (backend, frontend)
- Документацию
- Конфигурацию

## Использование через llmos

```bash
# Сбор контекста
./llmos collect backend-auth
./llmos collect frontend-ui
./llmos collect smart subscription billing

# PLAN режим (автоматически собирает контекст)
./llmos plan backend_refactor

# BUILD режим (использует собранный контекст)
./llmos build backend_refactor phase-1
```

## Интеграция с PLAN/BUILD

1. **PLAN режим:**
   - Скрипт автоматически собирает контекст
   - LLM читает весь контекст (НЕ по частям!)
   - Создает план: фазы, чек-лист, правила стиля
   - Записывает в `artifacts/PLAN_{task}_plan.md`

2. **BUILD режим:**
   - LLM читает план и контекст
   - Реализует ТОЛЬКО указанную фазу
   - Следует строго плану

## Примеры использования

### Пример 1: Рефакторинг backend authentication
```bash
# 1. Сбор контекста
./llmos collect backend-auth

# 2. Создание плана
./llmos plan backend_auth_refactor
# LLM читает artifacts/collected_backend_auth.md
# Создает artifacts/PLAN_backend_auth_refactor_plan.md

# 3. Реализация фазы 1
./llmos build backend_auth_refactor phase-1
```

### Пример 2: Редизайн frontend UI
```bash
# 1. Сбор контекста
./llmos collect frontend-ui

# 2. Создание плана
./llmos plan frontend_ui_redesign

# 3. Реализация по фазам
./llmos build frontend_ui_redesign phase-1
./llmos build frontend_ui_redesign phase-2
```

### Пример 3: Умный поиск
```bash
# Поиск всего, что связано с subscriptions и billing
./llmos collect smart subscription billing stripe
```

## Выходные файлы

Все собранные контексты сохраняются в `artifacts/`:
- `collected_backend_auth.md` - полный контекст backend auth
- `collected_frontend_ui.md` - полный контекст frontend UI
- `collected_{keywords}.md` - умный поиск
- `PLAN_{task}.md` - собранный контекст для задачи
- `PLAN_{task}_plan.md` - план рефакторинга

## Преимущества

1. **Полный контекст:** LLM видит всю систему, а не куски
2. **Нет туннельного зрения:** Все связи между компонентами видны
3. **Экономия токенов:** Один большой контекст эффективнее множества маленьких
4. **Планирование:** Возможность создать план до реализации
5. **Фазированная реализация:** Строгое следование плану по фазам

## См. также

- `PROJECT_CONFIG.md` - раздел "PLAN/BUILD ПОДХОД"
- `ROLES/02_architect.md` - PLAN/BUILD режимы для ARCHITECT
- `ROLES/04_backend_dev.md` - PLAN/BUILD режимы для BACKEND_DEV
- `ROLES/05_frontend_dev.md` - PLAN/BUILD режимы для FRONTEND_DEV
- `scenarios/DEEP_REFACTOR.yml` - сценарий для глубоких рефакторингов

