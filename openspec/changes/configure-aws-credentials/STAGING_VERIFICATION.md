# Staging Verification для configure-aws-credentials

**Change:** `configure-aws-credentials`  
**Статус:** ✅ Готов к архивации (все задачи выполнены)  
**Production тесты:** ⏸️ Отложены до финального апрува овнера

---

## Цель

Проверить работоспособность OIDC credentials и fallback механизма на **staging окружении** перед архивацией change.

---

## Предварительные требования

1. ✅ OIDC Provider настроен в AWS IAM
2. ✅ IAM роль `flowlogic-ci-cd-staging` создана
3. ✅ GitHub Environment `staging` настроен с `AWS_ROLE_ARN`
4. ✅ Workflow файлы обновлены (`.github/workflows/backend-deploy.yml`, `.github/workflows/ci-cd.yml`)

---

## Чек-лист для staging verification

### 1. Проверка OIDC аутентификации на staging

**Шаги:**
1. Запустить workflow `.github/workflows/backend-deploy.yml` на ветке `develop` (staging)
2. Проверить логи шага "Configure AWS credentials (OIDC)"
3. Убедиться что:
   - ✅ `AWS_ROLE_ARN` найден в GitHub Environment
   - ✅ OIDC токен получен успешно
   - ✅ AWS credentials настроены через OIDC

**Ожидаемый результат:**
```
✅ AWS_ROLE_ARN found in GitHub Environment
✅ Successfully configured AWS credentials via OIDC
```

**Команда для запуска:**
```bash
# Переключиться на ветку develop
git checkout develop

# Workflow запустится автоматически при push, или можно запустить вручную через GitHub Actions UI
```

---

### 2. Проверка деплоя на staging

**Шаги:**
1. Убедиться что staging deployment проходит успешно
2. Проверить что backend деплоится через OIDC
3. Проверить что все секреты доступны из GitHub Environment

**Ожидаемый результат:**
- ✅ Deployment успешно завершен
- ✅ Backend доступен на staging URL
- ✅ Health check endpoint возвращает `healthy`

---

### 3. Проверка fallback механизма (опционально)

**Шаги:**
1. Временно убрать `AWS_ROLE_ARN` из GitHub Environment `staging`
2. Запустить deployment
3. Проверить что fallback на Access Keys работает (если настроен)
4. Проверить что CloudWatch metric отправляется (если fallback используется)

**⚠️ Внимание:** Этот тест нужно выполнять только если fallback механизм критичен для staging. В идеале staging должен работать только через OIDC.

**Ожидаемый результат:**
- ✅ Fallback активируется только если OIDC не работает
- ✅ CloudWatch metric `CICD/FallbackAccessKeysUsed` обновляется
- ✅ Alert отправляется (если настроен)

**Важно:** После теста вернуть `AWS_ROLE_ARN` в GitHub Environment!

---

### 4. Проверка валидации credentials

**Шаги:**
1. Проверить что валидация `validate-aws-credentials.sh` работает
2. Убедиться что ошибки валидации корректно обрабатываются

**Ожидаемый результат:**
- ✅ Валидация проходит успешно при наличии `AWS_ROLE_ARN`
- ✅ Валидация падает с понятной ошибкой при отсутствии credentials

---

### 5. Проверка чтения секретов

**Шаги:**
1. Убедиться что секреты из GitHub Environment доступны в workflow
2. Проверить что `DATABASE_URL`, `API_KEY_PREFIX` загружаются корректно

**Ожидаемый результат:**
- ✅ Все секреты доступны в workflow
- ✅ Lambda функции получают корректные environment variables

---

## Результаты проверки

Заполнить после выполнения:

- [ ] OIDC аутентификация работает на staging
- [ ] Deployment на staging проходит успешно
- [ ] Валидация credentials работает корректно
- [ ] Секреты доступны из GitHub Environment
- [ ] Fallback механизм работает (если тестировался)

**Дата проверки:** __________  
**Проверяющий:** __________  
**Замечания:** __________

---

## После успешной проверки

1. ✅ Staging verification завершена успешно
2. ✅ Change готов к архивации
3. ⏸️ Production тесты отложены до финального апрува овнера
4. 📝 Создать документ с результатами для овнера

---

## Примечания

- Production тесты будут выполнены после полного окончания работы над проектом
- Требуется финальный апрув овнера перед production deployment
- Staging verification достаточна для архивации change
- Все production тесты (9.6-9.13) будут выполнены позже
