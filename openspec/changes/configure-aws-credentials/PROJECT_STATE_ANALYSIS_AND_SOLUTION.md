# Анализ состояния проекта и решение

**Дата:** 2026-01-18  
**Статус:** Анализ завершен, решение предложено

---

## 📊 ТЕКУЩЕЕ СОСТОЯНИЕ ПРОЕКТА

### CloudFormation Stacks

**Staging:**
- ✅ `flowlogic-backend-staging-v2-staging` (CREATE_COMPLETE) - новый stack, только deployment bucket
- ✅ `flowlogic-backend-staging-clean-staging-test` (CREATE_COMPLETE) - тестовый stack
- ❌ `flowlogic-backend-staging` (DELETE_FAILED) - требует очистки

**Production:**
- ✅ `flowlogic-backend-production` (UPDATE_COMPLETE) - активный, последнее обновление 2026-01-14
- ✅ `flowlogic-backend-prod` (UPDATE_COMPLETE) - активный, последнее обновление 2026-01-12
- ⚠️ **Дубликат**: Два production stack (prod и production)

**Dev:**
- ✅ Работает (не проверялся детально, но таблицы существуют)

---

### DynamoDB Tables

**Staging (6 таблиц существуют):**
- `flowlogic-staging-exercise-assessments-mapping`
- `flowlogic-staging-exercises`
- `flowlogic-staging-migrations` ⚠️ **КОНФЛИКТ**
- `flowlogic-staging-movement-tests`
- `flowlogic-staging-training-plan-templates`
- `flowlogic-staging-user-exercise-progress`

**Ожидаемые таблицы (из resources.yml):**
- `flowlogic-staging-users` ❌ не существует
- `flowlogic-staging-subscriptions` ❌ не существует
- `flowlogic-staging-assessments` ❌ не существует
- `flowlogic-staging-plans` ❌ не существует
- `flowlogic-staging-calendar-tasks` ❌ не существует
- `flowlogic-staging-progress` ❌ не существует
- `flowlogic-staging-user-limits` ❌ не существует
- `flowlogic-staging-migrations` ✅ существует (конфликт при создании)

**Production:**
- ⚠️ **Дубликаты**: `flowlogic-prod-*` и `flowlogic-production-*`
- Множество таблиц существуют

---

### S3 Buckets

**Staging:**
- 6 deployment buckets (требуется cleanup)
- `flowlogic-staging-videos` ❌ не существует (будет создан)

**Production:**
- 3 deployment buckets
- `flowlogic-prod-videos` ✅ существует
- `flowlogic-production-videos` ✅ существует (дубликат?)

---

### Lambda Functions

**Staging:**
- ❌ Нет Lambda функций (т.к. stack содержит только bucket)

**Production:**
- ✅ `flowlogic-backend-production-api` (активен)
- ✅ `flowlogic-backend-prod-api` (активен, дубликат?)

---

### API Gateway

**Существующие:**
- `dev-flowlogic-backend` (id: t1p7ii26f5)
- `prod-flowlogic-backend` (id: 4yei7a5aig)
- `production-flowlogic-backend` (id: ip7pe8lsf7)

**Staging:**
- ❌ Нет отдельного staging API Gateway

---

## 🔍 НАЙДЕННЫЕ ПРОБЛЕМЫ

### 1. Критическая: Конфликт ресурсов при деплое на staging

**Проблема:**
- Таблица `flowlogic-staging-migrations` уже существует
- CloudFormation не может создать существующий ресурс
- Все остальные ресурсы отменяются при откате

**Причина:**
- Таблицы были созданы ранее вручную или другим stack
- Текущий stack пытается создать их заново

---

### 2. Архитектурная: Дубликаты в production

**Проблема:**
- Два production stack: `flowlogic-backend-prod` и `flowlogic-backend-production`
- Два набора таблиц: `flowlogic-prod-*` и `flowlogic-production-*`
- Два API Gateway для production

**Причина:**
- Изменение naming convention (prod → production)
- Старые ресурсы не были удалены

---

### 3. Техническая: Временный service name

**Проблема:**
- Service name изменен на `flowlogic-backend-staging-v2`
- Это временное решение для обхода DELETE_FAILED stack
- Не соответствует стандартной конвенции именования

---

### 4. Операционная: Старые failed stacks

**Проблема:**
- `flowlogic-backend-staging` в состоянии DELETE_FAILED
- Блокирует использование стандартного имени
- Требуется ручная очистка

---

## ✅ ПРЕДЛОЖЕННОЕ РЕШЕНИЕ

### Этап 1: Очистка и подготовка (КРИТИЧНО)

#### 1.1 Очистка DELETE_FAILED stack

```bash
# Получить список заблокированных ресурсов
aws cloudformation describe-stack-resources \
  --stack-name flowlogic-backend-staging \
  --query 'StackResources[?ResourceStatus==`DELETE_FAILED`]'

# Очистить заблокированные ресурсы вручную (если есть)
# Затем принудительно удалить stack
aws cloudformation delete-stack \
  --stack-name flowlogic-backend-staging \
  --deletion-mode FORCE_DELETE_STACK
```

#### 1.2 Разрешение конфликта с существующими таблицами

**Вариант A (Рекомендуемый): Использовать существующие таблицы**

Изменить CloudFormation template чтобы **не создавать** существующие таблицы:

1. Добавить проверку существования таблицы перед созданием
2. Использовать CloudFormation `DeletionPolicy: Retain` для критичных таблиц
3. Создать только отсутствующие таблицы

**Вариант B: Удалить и пересоздать**

```bash
# ⚠️ ВНИМАНИЕ: Потеря данных!
aws dynamodb delete-table --table-name flowlogic-staging-migrations
# Дождаться удаления
aws dynamodb wait table-not-exists --table-name flowlogic-staging-migrations
```

**Вариант C: Изменить имена таблиц (НЕ РЕКОМЕНДУЕТСЯ)**

Изменить naming convention, что потребует изменения кода приложения.

---

### Этап 2: Восстановление стандартного service name

#### 2.1 Удалить временные stacks

```bash
# Удалить тестовый stack
aws cloudformation delete-stack \
  --stack-name flowlogic-backend-staging-clean-staging-test

# Удалить v2 stack (после переноса данных если нужно)
aws cloudformation delete-stack \
  --stack-name flowlogic-backend-staging-v2-staging
```

#### 2.2 Вернуть стандартный service name

```yaml
# infra/serverless/serverless.yml
service: flowlogic-backend  # Вернуть обратно
```

---

### Этап 3: Исправление CloudFormation template

#### 3.1 Добавить DeletionPolicy для существующих таблиц

```yaml
# infra/serverless/resources.yml
MigrationsTable:
  Type: AWS::DynamoDB::Table
  DeletionPolicy: Retain  # Не удалять при удалении stack
  Properties:
    TableName: ${self:custom.tables.migrations}
    # ... остальные свойства
```

#### 3.2 Использовать существующие ресурсы вместо создания новых

Изменить workflow для проверки существования ресурсов:

```bash
# В .github/workflows/backend-deploy.yml
# Перед create-stack проверить существующие таблицы
# Если таблица существует - пропустить создание или использовать существующую
```

---

### Этап 4: Очистка дубликатов в production (Опционально)

#### 4.1 Определить активный production stack

- Проверить какой stack используется в production
- Проверить какие ресурсы активны (Lambda, API Gateway)

#### 4.2 Миграция данных из старого stack

Если `flowlogic-backend-prod` - старый:
1. Экспортировать данные из `flowlogic-prod-*` таблиц
2. Импортировать в `flowlogic-production-*` таблицы
3. Обновить DNS/API Gateway на новый stack
4. Удалить старый stack

---

## 🚀 ПЛАН ВЫПОЛНЕНИЯ (Приоритеты)

### Критично (Блокирует деплой на staging):

1. ✅ **Очистка DELETE_FAILED stack**
   - Время: 5-10 минут
   - Риск: Низкий
   - Действие: Принудительное удаление

2. ✅ **Разрешение конфликта с `flowlogic-staging-migrations`**
   - Время: 15-30 минут
   - Риск: Средний (потеря данных если удалять)
   - Действие: Использовать существующую таблицу (Вариант A) или удалить (Вариант B)

3. ✅ **Исправление CloudFormation template**
   - Время: 30-60 минут
   - Риск: Низкий
   - Действие: Добавить DeletionPolicy и проверки существования

### Важно (Улучшение архитектуры):

4. **Восстановление стандартного service name**
   - Время: 10-15 минут
   - Риск: Низкий
   - Действие: Вернуть `service: flowlogic-backend`

5. **Очистка тестовых stacks**
   - Время: 5-10 минут
   - Риск: Низкий
   - Действие: Удалить тестовые stacks

### Опционально (Улучшение production):

6. **Очистка дубликатов в production**
   - Время: 2-4 часа (с миграцией данных)
   - Риск: Высокий (production downtime)
   - Действие: Планировать отдельно, не блокирует staging деплой

---

## 📋 РЕКОМЕНДУЕМОЕ РЕШЕНИЕ (Кратко)

### Для немедленного деплоя на staging:

1. **Очистить DELETE_FAILED stack** через AWS Console или CLI
2. **Использовать существующие таблицы** - добавить `DeletionPolicy: Retain` и проверки
3. **Создать только отсутствующие таблицы** через отдельный скрипт или CloudFormation custom resource
4. **Протестировать деплой** на staging

### Для долгосрочного решения:

1. **Восстановить стандартный service name** после очистки
2. **Добавить проверки существования ресурсов** в workflow
3. **Документировать процесс миграции** для production
4. **Очистить дубликаты** в отдельном maintenance window

---

## ⚠️ РИСКИ И ПРЕДУПРЕЖДЕНИЯ

1. **Удаление существующих таблиц приведет к потере данных**
   - Рекомендуется: использовать существующие таблицы (Вариант A)

2. **Production имеет дубликаты**
   - Не трогать production без детального плана миграции
   - Текущий production работает, не менять без необходимости

3. **Временный service name**
   - Вернуть стандартный после решения конфликтов
   - Обновить все ссылки и документацию

---

**Статус:** Анализ завершен, решение предложено. Готов к реализации.
