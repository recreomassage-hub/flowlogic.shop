# LLM-OS System
*.log
*.tmp
*.cache

# IDE
.vscode/
.idea/
*.swp
*.swo

# Dependencies
node_modules/
__pycache__/
*.pyc
*.pyo

# Environment
.env
.env.local
.env.development.local
.env.test.local
.env.production
.env.production.local
.env.*.local
.secrets/

# Reports
reports/*.html
reports/*.pdf

# Artifacts (собранные контексты и планы)
artifacts/*.md
!artifacts/README.md

# Temporary
tmp/
temp/

# Obsidian
.obsidian/
*.obsidian

# SSH keys and certificates (CRITICAL: Never commit private keys!)
*.pem
*.key
*.p12
*.pfx
*.crt
*.cer
id_rsa
id_rsa.pub
id_ed25519
id_ed25519.pub
id_ecdsa
id_ecdsa.pub
id_dsa
id_dsa.pub
*.pub
known_hosts
authorized_keys
*.ppk
ssh_config
# Исключаем только корневую config/ (SSH config)
/config
# Но НЕ исключаем src/backend/config/ и src/frontend/config/
!/src/**/config/

id_*
*.key
*.pem
.obsidian/

# Serverless Framework
.serverless/
gitleaks-*.json
*-analysis.json

# AWS CLI (не должен быть в репозитории)
aws/dist/
aws/
awscliv2.zip
*.zip
!node_modules/**/*.zip  # Разрешаем zip в node_modules (зависимости)

# AWS CLI installation files
awscliv2/
awscli/
aws-cli/

# AWS config files (кроме примеров)
.aws/
aws-credentials
aws-config
*aws*.config
!**/aws-config.example

# Bug Fixer
.bug-locks/
.bug-fixer/logs/
!.bug-fixer/config.yaml
!.bug-fixer/README.md