# 🔒 Проверка безопасности .env файла

## ✅ Правильный подход

**Да, можно переименовать `.env.example` в `.env` и добавить токен!**

Это стандартная практика:
1. `.env.example` — шаблон (хранится в Git)
2. `.env` — ваша локальная конфигурация (НЕ в Git)

---

## ⚠️ Критические проверки

### 1. .env должен быть в .gitignore

```bash
# Проверка
git check-ignore .env
# Должно вывести: .env
```

✅ **У вас:** `.env` в `.gitignore` — безопасно!

### 2. .env НЕ должен отслеживаться Git

```bash
# Проверка
git ls-files .env
# Не должно вывести ничего
```

⚠️ **Если .env отслеживается Git:**
```bash
# Удалить из Git (но оставить локально)
git rm --cached .env
```

### 3. .env.example должен остаться в репозитории

```bash
# Проверка
git ls-files .env.example
# Должно вывести: .env.example
```

✅ **У вас:** `.env.example` существует — хорошо!

---

## 🚨 Что делать, если .env попал в Git

### Если .env был закоммичен:

1. **Удалите из Git (но оставьте локально):**
   ```bash
   git rm --cached .env
   ```

2. **Закоммитьте удаление:**
   ```bash
   git commit -m "Remove .env from Git (contains secrets)"
   ```

3. **Если токен уже в истории Git:**
   - **НЕМЕДЛЕННО отзовите токен в GitHub:**
     - Settings → Developer settings → Personal access tokens
     - Удалите скомпрометированный токен
   - **Создайте новый токен**
   - **Очистите историю Git** (если нужно):
     ```bash
     # Используйте git-filter-repo (см. SECURITY_FIX.md)
     git filter-repo --invert-paths --path .env
     ```

---

## ✅ Правильная структура

```
.
├── .env              # Локальный файл (НЕ в Git)
├── .env.example      # Шаблон (в Git)
└── .gitignore        # Содержит .env
```

---

## 📋 Чеклист безопасности

- [ ] `.env` существует локально
- [ ] `.env` в `.gitignore`
- [ ] `.env` НЕ отслеживается Git (`git ls-files .env` пусто)
- [ ] `.env.example` существует и в Git
- [ ] Токен добавлен в `.env` (локально)
- [ ] Токен НЕ в `.env.example` (шаблон)

---

## 🔐 Дополнительные меры безопасности

1. **Ротация токенов:**
   - Токены должны ротироваться каждые 90 дней
   - См. `docs/security/policies.md`

2. **Минимальные права:**
   - Используйте только необходимые scopes
   - Не давайте токену больше прав, чем нужно

3. **Мониторинг:**
   - Используйте GitGuardian для сканирования
   - GitHub Secret Scanning включен

---

**Обновлено:** 2025-12-23



