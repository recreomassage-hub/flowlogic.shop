# 🔄 Миграция на сценарный режим

**Дата создания:** 2025-12-26  
**Цель:** Переход от ручного управления к автоматическому

---

## 🎯 ЧТО ИЗМЕНИЛОСЬ

### Было:
```
Человек → думает → запускает скрипты → агенты импровизируют
```

### Стало:
```
Событие → сценарий → автомат → агенты действуют в рамках
```

---

## 📋 КАК НАЧАТЬ

### Шаг 1: Проверьте текущий сценарий
```bash
./llmos scenario status
```

### Шаг 2: Выполните сценарий
```bash
./llmos run
```

**Что происходит:**
- Система читает `SCENARIO_STATE.yml`
- Определяет текущий сценарий и фазу
- Показывает разрешенные действия
- Агент работает только в рамках `allowed_actions`

---

## 🧩 ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ

### Пример 1: Разработка фичи

```bash
# 1. Установить сценарий
./llmos scenario set FEATURE_DEVELOPMENT BRIEF

# 2. Выполнить сценарий
./llmos run
# Показывает: что делать в фазе BRIEF

# 3. Агент работает в Cursor
# (только allowed_actions: create_feature_brief, update_docs)

# 4. Переход к следующей фазе
./llmos scenario next
./llmos run
# Теперь фаза: IMPACT_ANALYSIS
```

### Пример 2: Автоматический инцидент

```bash
# Система обнаруживает deploy failure
./scripts/incident_detector.sh check

# Автоматически переключает на:
# INCIDENT_RECOVERY:FREEZE

# Выполнение (автоматически, без диалогов)
./llmos run
# → FREEZE → ROLLBACK → VERIFY
```

### Пример 3: Quality Gate

```bash
# Установить сценарий
./llmos scenario set QUALITY_GATE AUDIT

# Выполнить
./llmos run
# Показывает: что проверять

# Агент: QUALITY_AUDITOR
# Может только: читать артефакты, оценивать, генерировать отчет
# НЕ может: менять код, менять сценарий
```

---

## 🔒 ВАЖНЫЕ ИЗМЕНЕНИЯ

### Для агентов:

**Раньше:**
```
Агент мог решать, что делать дальше
```

**Теперь:**
```
Агент следует только allowed_actions из SCENARIO_STATE.yml
```

### Для человека:

**Раньше:**
```
Нужно было думать, какой скрипт запустить
```

**Теперь:**
```
Просто: ./llmos run
Система сама определяет, что делать
```

---

## 📚 ДОПОЛНИТЕЛЬНЫЕ РЕСУРСЫ

- **Справочник сценариев:** `scenarios_reference.md`
- **Ограничения агентов:** `agent_constraints.md`
- **SCENARIO_STATE.yml** - Текущее состояние

---

**Последнее обновление:** 2025-12-26


