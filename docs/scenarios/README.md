# 🎛️ Сценарный режим LLM-OS

**Дата создания:** 2025-12-26  
**Цель:** Автоматическое управление процессами через сценарии

---

## 🎯 ПРИНЦИП

**Было:**
```
Человек → думает → запускает скрипты → агенты импровизируют
```

**Стало:**
```
Событие → сценарий → автомат → агенты действуют в рамках
```

---

## 📋 7 КАНОНИЧЕСКИХ СЦЕНАРИЕВ

| ID | Сценарий | Когда |
|----|----------|-------|
| S1 | PROJECT_BOOTSTRAP | Старт проекта |
| S2 | FEATURE_DEVELOPMENT | Разработка фичи |
| S3 | DEPLOYMENT | Деплой |
| S4 | INCIDENT_RECOVERY | Ошибка/инцидент |
| S5 | QUALITY_GATE | Проверка качества |
| S6 | ROLLBACK | Откат |
| S7 | MAINTENANCE | Поддержка |

**📌 Больше не нужно.**

---

## 🎮 ИСПОЛЬЗОВАНИЕ

### Автоматическое выполнение:
```bash
./llmos run
```

**Что делает:**
- Читает `SCENARIO_STATE.yml`
- Определяет текущий сценарий и фазу
- Выполняет соответствующие действия
- Показывает разрешенные действия для агентов

### Проверка статуса:
```bash
./llmos scenario status
# или
./llmos status  # Показывает WORKFLOW_STATE
```

### Установка сценария (только системные события):
```bash
./llmos scenario set FEATURE_DEVELOPMENT BRIEF
```

### Переход к следующей фазе:
```bash
./llmos scenario next
```

---

## 🔒 ОГРАНИЧЕНИЯ АГЕНТОВ

### ❌ АГЕНТ НЕ МОЖЕТ:
- Менять сценарий (`current_scenario`)
- Менять фазу (`phase`)
- Запускать скрипты напрямую
- Импровизировать процессные решения

### ✅ АГЕНТ МОЖЕТ:
- Выполнять `allowed_actions`
- Обновлять артефакты (код, документация)
- Фиксировать результат
- Вызывать `step.sh` для коммита (в разрешенных фазах)

---

## 🧩 ПРИМЕРЫ

### Пример 1: Разработка фичи
```bash
# 1. Установить сценарий
./llmos scenario set FEATURE_DEVELOPMENT BRIEF

# 2. Выполнить сценарий
./llmos run
# Показывает: что делать в фазе BRIEF

# 3. Агент работает в Cursor (только allowed_actions)

# 4. Переход к следующей фазе
./llmos scenario next
./llmos run
```

### Пример 2: Инцидент (автоматически)
```bash
# Система обнаруживает ошибку → автоматически
./llmos scenario set INCIDENT_RECOVERY FREEZE

# Выполнение (автоматически, без диалогов)
./llmos run
# → FREEZE → ROLLBACK → VERIFY
```

---

## 📚 ДОКУМЕНТАЦИЯ

- **Справочник сценариев:** `scenarios_reference.md` - Полное описание всех сценариев
- **SCENARIO_STATE.yml** - Текущее состояние системы
- **scenario_engine.sh** - Движок сценариев

---

**Последнее обновление:** 2025-12-26

