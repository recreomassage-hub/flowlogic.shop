# Аудит Spec-Driven Development интеграции

**Дата:** 2025-01-03  
**Проверка:** Дублирование функций, логические ошибки, противоречия

---

## 🔍 ОБНАРУЖЕННЫЕ ПРОБЛЕМЫ

### 1. ⚠️ ДУБЛИРОВАНИЕ: Два разных подхода к планированию

#### Проблема:
Существуют **ДВА параллельных workflow** для планирования:

**A. Существующий PLAN/BUILD (LLM-OS):**
- Команда: `./llmos plan {task_name}`
- Контекст: `artifacts/PLAN_{task_name}.md` (собранный через collect-скрипты)
- План: `artifacts/PLAN_{task_name}_plan.md`
- Использование: `./llmos build {task_name} phase-1`
- Описан в: `ROLES/02_architect.md`, `ROLES/04_backend_dev.md`, `ROLES/05_frontend_dev.md`

**B. Новый Spec-Driven (Spec Kit):**
- Команда: `/plan` (через Cursor)
- Контекст: `.specify/constitution.md` + `.specify/features/{name}/spec.md`
- План: `.specify/features/{name}/plan.md`
- Использование: `/implement` с tasks.md
- Описан в: `.cursor/commands/plan.md`, `.cursor/commands/implement.md`

#### Конфликт:
- **Разные пути хранения:** `artifacts/PLAN_*` vs `.specify/features/*/plan.md`
- **Разные форматы:** Один план в artifacts, другой в .specify
- **Разные команды:** `./llmos plan` vs `/plan` в Cursor
- **Разные источники контекста:** collect-скрипты vs constitution.md + spec.md

#### Рекомендация:
**НЕ конфликт, но нужна ясность:**
- `artifacts/PLAN_*` - для быстрых рефакторингов через `./llmos plan`
- `.specify/features/*/plan.md` - для новых фич через Spec-Driven workflow
- **Уточнить в документации:** когда использовать какой подход

---

### 2. ⚠️ ПРОТИВОРЕЧИЕ: Структура планов

#### Проблема:
**ROLES/02_architect.md** говорит:
```
Запиши в `artifacts/PLAN_{task_name}_plan.md`
```

**Spec-Driven** говорит:
```
Создай `.specify/features/{name}/plan.md`
```

#### Конфликт:
- Архитектор может создать план в `artifacts/`, но Spec-Driven ожидает `.specify/`
- Неясно, какой путь использовать для новых фич

#### Рекомендация:
**Уточнить в ROLES/02_architect.md:**
- Для новых фич: использовать `.specify/features/{name}/plan.md`
- Для рефакторингов: использовать `artifacts/PLAN_{task}_plan.md`

---

### 3. ⚠️ ДУБЛИРОВАНИЕ: Команда "plan"

#### Проблема:
**Существующая команда:**
```bash
./llmos plan backend_refactor
```
- Собирает контекст через collect-скрипты
- Создает план в `artifacts/PLAN_*_plan.md`

**Новая команда:**
```
/plan

@constitution.md @spec.md @clarifications.md
```
- Читает constitution и spec
- Создает план в `.specify/features/{name}/plan.md`

#### Конфликт:
- Одинаковое название "plan", но разные процессы
- Может вызвать путаницу

#### Рекомендация:
**Переименовать или уточнить:**
- `./llmos plan` → остается для быстрых рефакторингов
- `/plan` → для Spec-Driven workflow (новые фичи)
- **Добавить в документацию:** четкое разделение use cases

---

### 4. ⚠️ ПРОТИВОРЕЧИЕ: Источники контекста

#### Проблема:
**Существующий подход (PLAN режим):**
```
./llmos collect backend-auth  → artifacts/PLAN_backend-auth.md
./llmos plan backend-auth     → читает artifacts/PLAN_backend-auth.md
```

**Новый подход (Spec-Driven):**
```
/specify → .specify/features/{name}/spec.md
/plan    → читает .specify/constitution.md + spec.md
```

#### Конфликт:
- Разные источники контекста
- Constitution может не содержать детали, которые есть в collect-скриптах
- Spec.md может не содержать существующий код, который есть в collect-скриптах

#### Рекомендация:
**Интегрировать:**
- `/plan` должен также читать `artifacts/PLAN_*.md` если существует
- Или использовать `./llmos collect` перед `/plan` для новых фич

---

### 5. ⚠️ ЛОГИЧЕСКАЯ ОШИБКА: Constitution vs PROJECT_CONFIG.md

#### Проблема:
**Constitution говорит:**
```
PROJECT_CONFIG.md = ROM (Read-Only Memory)
```

**Но Constitution сам содержит:**
- Stack & Versions (дублирует tech_stack.md)
- Naming Conventions (может конфликтовать с PROJECT_CONFIG.md)
- Architecture Principles (может конфликтовать с ROLES/*.md)

#### Конфликт:
- Два источника истины для правил проекта
- Constitution может устареть, если не синхронизировать с PROJECT_CONFIG.md

#### Рекомендация:
**Уточнить иерархию:**
- `PROJECT_CONFIG.md` = Высокоуровневые правила LLM-OS
- `.specify/constitution.md` = Детальные технические стандарты кода
- Constitution должен ссылаться на PROJECT_CONFIG.md как источник

---

### 6. ⚠️ ПРОТИВОРЕЧИЕ: Naming Conventions

#### Проверка:
**Constitution говорит:**
- API Routes: kebab-case (`/api/v1/auth/register`)
- Database Tables: kebab-case (`flowlogic-production-users`)

**Проверка реального кода:**
- ✅ `/v1/auth/register` - соответствует
- ✅ `flowlogic-production-users` - соответствует

**Вывод:** ✅ Нет противоречий

---

### 7. ⚠️ ПРОТИВОРЕЧИЕ: Library Rules

#### Проверка:
**Constitution говорит:**
- ❌ Axios в backend (use native fetch or AWS SDK)

**Проверка реального кода:**
- Backend: Использует AWS SDK, нет Axios ✅
- Frontend: Использует Axios ✅

**Вывод:** ✅ Нет противоречий

---

### 8. ⚠️ ДУБЛИРОВАНИЕ: Workflow описание

#### Проблема:
**Spec-Driven workflow описан в:**
1. `.specify/README.md` (5 этапов)
2. `.cursor/commands/*.md` (детали каждого этапа)
3. `docs/planning/spec_driven_integration_plan.md` (полный план)
4. `.cursorrules` (правила использования)

#### Конфликт:
- Много мест с описанием одного и того же
- Может быть несинхронизация

#### Рекомендация:
**Иерархия документации:**
- `.specify/README.md` = Краткий обзор
- `.cursor/commands/*.md` = Детали команд
- `docs/planning/spec_driven_integration_plan.md` = Полный план интеграции
- `.cursorrules` = Правила для AI

---

### 9. ⚠️ ЛОГИЧЕСКАЯ ОШИБКА: Clarify команда

#### Проблема:
**`.cursor/commands/clarify.md` говорит:**
```
AI анализирует неясные моменты
AI создает/обновляет clarifications.md
```

**Но в примере:**
```
/clarify

@spec.md

Review the specification and clarify:
1. {вопрос 1}
```

#### Конфликт:
- Неясно: AI сам находит вопросы или разработчик задает?
- Если разработчик задает - зачем AI "анализирует"?

#### Рекомендация:
**Уточнить процесс:**
- Вариант A: Разработчик задает вопросы → AI отвечает
- Вариант B: AI анализирует spec → находит неясности → задает вопросы
- **Выбрать один вариант и документировать четко**

---

### 10. ⚠️ ПРОТИВОРЕЧИЕ: Tasks vs BUILD режим

#### Проблема:
**Существующий BUILD режим:**
```
./llmos build {task} phase-1
```
- Читает `artifacts/PLAN_{task}_plan.md`
- Реализует фазу phase-1

**Новый Spec-Driven:**
```
/implement

Task: 1
```
- Читает `.specify/features/{name}/tasks.md`
- Реализует Task 1

#### Конфликт:
- Разные форматы разбивки: "phases" vs "tasks"
- Разные команды для одного действия

#### Рекомендация:
**Унифицировать или разделить:**
- BUILD режим = для рефакторингов (phases)
- Implement = для новых фич (tasks)
- **Добавить в документацию:** четкое разделение

---

## ✅ ЧТО РАБОТАЕТ ПРАВИЛЬНО

### 1. Constitution соответствует коду
- ✅ Naming conventions проверены
- ✅ Library rules соответствуют package.json
- ✅ Architecture principles соответствуют структуре

### 2. Команды не конфликтуют с существующими
- ✅ `/specify`, `/clarify`, `/plan`, `/tasks`, `/implement` - новые команды
- ✅ `./llmos plan`, `./llmos build` - остаются для рефакторингов

### 3. Структура файлов логична
- ✅ `.specify/constitution.md` - централизованные правила
- ✅ `.specify/features/{name}/` - изолированные фичи
- ✅ `.cursor/commands/` - команды для Cursor

---

## 📋 РЕКОМЕНДАЦИИ ПО ИСПРАВЛЕНИЮ

### Приоритет 1: Критичные противоречия

1. **Уточнить иерархию источников правил:**
   - `PROJECT_CONFIG.md` = LLM-OS правила
   - `.specify/constitution.md` = Технические стандарты кода
   - Добавить ссылку в constitution на PROJECT_CONFIG.md

2. **Разделить use cases для PLAN:**
   - `./llmos plan` = Быстрые рефакторинги (artifacts/)
   - `/plan` = Новые фичи (spec-driven, .specify/)
   - Обновить ROLES/*.md с указанием когда использовать какой

3. **Уточнить процесс Clarify:**
   - Выбрать: AI анализирует ИЛИ разработчик задает вопросы
   - Обновить `.cursor/commands/clarify.md`

### Приоритет 2: Улучшения

4. **Интегрировать collect-скрипты с Spec-Driven:**
   - `/plan` может использовать `./llmos collect` для сбора контекста
   - Или добавить шаг "collect context" в Spec-Driven workflow

5. **Унифицировать терминологию:**
   - "phases" (BUILD) vs "tasks" (Spec-Driven)
   - Добавить глоссарий или использовать один термин

6. **Синхронизация документации:**
   - `.specify/README.md` как главный источник
   - Остальные ссылаются на него

---

## 📊 СВОДНАЯ ТАБЛИЦА ПРОТИВОРЕЧИЙ

| # | Проблема | Тип | Критичность | Статус |
|---|----------|-----|-------------|--------|
| 1 | Два workflow для планирования | Дублирование | Средняя | Требует уточнения |
| 2 | Разные пути для планов | Противоречие | Средняя | Требует уточнения |
| 3 | Команда "plan" дублируется | Дублирование | Низкая | Требует уточнения |
| 4 | Разные источники контекста | Противоречие | Средняя | Требует интеграции |
| 5 | Constitution vs PROJECT_CONFIG | Противоречие | Высокая | Требует уточнения |
| 6 | Naming conventions | Проверка | - | ✅ OK |
| 7 | Library rules | Проверка | - | ✅ OK |
| 8 | Множественное описание workflow | Дублирование | Низкая | Требует синхронизации |
| 9 | Clarify процесс неясен | Логическая ошибка | Средняя | Требует уточнения |
| 10 | Tasks vs Phases | Противоречие | Средняя | Требует уточнения |

---

## 🎯 ИТОГОВАЯ ОЦЕНКА

### ✅ Сильные стороны:
- Constitution соответствует реальному коду
- Структура файлов логична
- Команды не конфликтуют напрямую

### ⚠️ Требует доработки:
- Иерархия источников правил (PROJECT_CONFIG vs Constitution)
- Разделение use cases для PLAN/BUILD vs Spec-Driven
- Процесс Clarify
- Интеграция collect-скриптов с Spec-Driven

### 🔴 Критичные проблемы:
- **НЕТ критичных блокеров** - система работоспособна
- Все проблемы решаются уточнением документации

---

## 📝 ПЛАН ИСПРАВЛЕНИЙ

### Шаг 1: Уточнить иерархию (5 мин)
- Добавить в constitution ссылку на PROJECT_CONFIG.md
- Уточнить: PROJECT_CONFIG = LLM-OS, Constitution = Code Standards

### Шаг 2: Разделить use cases (10 мин)
- Обновить ROLES/*.md с указанием:
  - `./llmos plan` для рефакторингов
  - `/plan` для новых фич через Spec-Driven

### Шаг 3: Уточнить Clarify (5 мин)
- Выбрать процесс (AI анализирует ИЛИ разработчик задает)
- Обновить `.cursor/commands/clarify.md`

### Шаг 4: Добавить интеграцию (опционально)
- `/plan` может использовать `./llmos collect` если нужно

---

**Версия:** 1.0  
**Статус:** Аудит завершен, готов к исправлениям

