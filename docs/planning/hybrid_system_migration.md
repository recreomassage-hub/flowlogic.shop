# Гибридная система: Spec-Driven + Issue Tracking

**Дата:** 2026-01-06  
**Статус:** Внедрено  
**Версия:** 1.0

---

## 📋 ОБЗОР

Внедрена гибридная система, объединяющая **Spec-Driven Development** (для проектирования) и **Issue Tracking** (для исполнения), вдохновленная подходом Beads.

### Проблемы, которые решает

❌ **До:**
- Markdown планы теряются (605 файлов планов)
- Агенты забывают контекст между сессиями
- Сложно отслеживать зависимости задач
- Обнаруженные проблемы не фиксируются

✅ **После:**
- Структурированные данные (JSONL)
- Queryable (bd ready --json)
- Версионирование через git
- Граф зависимостей
- Обнаруженные проблемы → автоматические задачи

---

## 🏗️ АРХИТЕКТУРА

### Структура файлов

```
# Для НОВЫХ фич (проектирование)
.specify/features/{name}/
├── spec.md           ← Требования (как было)
├── plan.md           ← Технический план (как было)
└── tasks.md          ← Задачи (markdown, как было)

# Для ТЕКУЩЕЙ работы (исполнение)
.beads/
├── issues.jsonl      ← Все задачи в структурированном виде
└── status.json       ← Текущее состояние проекта (автогенерация)

# Твой "якорь"
STATUS.md             ← Человеко-читаемый обзор (автогенерация)
```

### Инструменты

- **`scripts/bd.sh`** - CLI для управления задачами
- **`scripts/generate-status.sh`** - Генерация STATUS.md
- **`.cursor/commands/create-issues.md`** - Команда для конвертации tasks.md → issues.jsonl

---

## 🔄 WORKFLOW

### 1. Проектирование новой фичи (Spec-Driven)

**Как было (сохраняется):**

```
/specify → spec.md
/clarify → clarifications.md
/plan → plan.md
/tasks → tasks.md
```

**НОВОЕ:**

```
/create-issues → .beads/issues.jsonl
```

Конвертирует задачи из `tasks.md` в структурированные issues.

### 2. Реализация (Issue Tracking)

**НОВОЕ:**

```bash
# Начало сессии
./scripts/bd.sh ready              # Что делать?
./scripts/bd.sh show {issue-id}   # Детали задачи

# Во время работы
./scripts/bd.sh start {issue-id}  # Начал работу
./scripts/bd.sh discover "problem" --from {issue-id}  # Обнаружил проблему
./scripts/bd.sh complete {issue-id} # Закончил

# Конец сессии
./scripts/generate-status.sh       # Обновить STATUS.md
git commit
```

### 3. Между сессиями

**Автоматически:**
- `.beads/issues.jsonl` версионируется в git
- `STATUS.md` генерируется из issues
- Контекст **никогда не теряется**

**При начале новой сессии:**
1. Читаешь `STATUS.md` - твой "якорь"
2. Проверяешь `.beads/status.json` - текущее состояние
3. Используешь `./scripts/bd.sh ready` - что делать дальше

---

## 📊 ФОРМАТ ISSUE

```json
{
  "id": "DS-4",
  "title": "Create Typography Components",
  "status": "ready" | "in_progress" | "done",
  "priority": 1-3,
  "epic": "design-system",
  "estimated_time": "1h",
  "blocks": ["DS-7", "DS-8"],
  "blocked_by": ["DS-1"],
  "discovered_from": "DS-2",  // опционально
  "created": "2026-01-06T00:00:00Z",
  "updated": "2026-01-06T00:00:00Z"
}
```

---

## 🎯 ПРАВИЛА РАБОТЫ

### Для AI агентов

**См. `AGENTS.md`** для полных правил.

**Ключевые принципы:**
- ✅ Spec-Driven для дизайна (не заменяется)
- ✅ Issue-tracking для исполнения (дополняет)
- ✅ Одна задача на сессию
- ✅ Всегда логировать обнаруженные проблемы
- ✅ Всегда обновлять статус перед завершением

### Для разработчиков

**См. `CONTRIBUTING.md`** для полного workflow.

**Быстрый старт:**
```bash
# Начало работы
cat STATUS.md                    # Прочитать текущее состояние
./scripts/bd.sh ready            # Увидеть готовые задачи
./scripts/bd.sh start {issue-id} # Начать работу

# Во время работы
./scripts/bd.sh discover "problem" --from {issue-id}

# Завершение
./scripts/bd.sh complete {issue-id}
./scripts/generate-status.sh
git commit
```

---

## 📈 МИГРАЦИЯ

### Текущее состояние

✅ **Внедрено:**
- Структура `.beads/`
- CLI инструменты (`bd.sh`, `generate-status.sh`)
- Миграция tasks из design-system в issues.jsonl
- STATUS.md автогенерация
- AGENTS.md с правилами
- CONTRIBUTING.md обновлен

### Следующие шаги

**Фаза 2 (опционально):**
- Git pre-commit hook для автообновления STATUS.md
- Визуализация графа зависимостей
- Автоопределение ready tasks (учитывая статус blocked_by)

**Фаза 3 (опционально):**
- Интеграция с GitHub Issues (если нужно)
- Web UI для просмотра issues (если нужно)

---

## ✅ ПРЕИМУЩЕСТВА

### Для проектирования

- ✅ Spec-Driven сохраняется (работает для новых фич)
- ✅ Spec → Plan → Tasks логичен для проектирования
- ✅ Нужна спецификация для сложных фич
- ✅ Constitution как "контракт качества"

### Для исполнения

- ✅ Структурированные данные (JSONL)
- ✅ Queryable (bd ready --json)
- ✅ Версионирование через git
- ✅ Граф зависимостей
- ✅ Обнаруженные проблемы → автоматические задачи

### Для работы урывками

- ✅ STATUS.md как "якорь" между сессиями
- ✅ Контекст никогда не теряется
- ✅ Агенты не "забывают" задачи
- ✅ Работа в свободное время = комфортно

---

## 📚 ДОКУМЕНТАЦИЯ

- **`AGENTS.md`** - Правила для AI агентов
- **`CONTRIBUTING.md`** - Workflow для разработчиков
- **`.cursor/commands/create-issues.md`** - Команда для создания issues
- **`STATUS.md`** - Текущее состояние (автогенерация)

---

## 🔧 TROUBLESHOOTING

### Проблема: `bd.sh` не работает

**Решение:**
```bash
chmod +x scripts/bd.sh
# Проверь что jq установлен: which jq
```

### Проблема: STATUS.md не обновляется

**Решение:**
```bash
./scripts/generate-status.sh
# Проверь что .beads/issues.jsonl существует
```

### Проблема: Issues не показываются как ready

**Решение:**
```bash
# Проверь блокировки
./scripts/bd.sh show {issue-id}

# Проверь статус
cat .beads/issues.jsonl | jq '.status'
```

---

**Последнее обновление:** 2026-01-06



