# 🎯 Как работать над проектом (Guide для работы в свободное время)

**Дата создания:** 2026-01-06  
**Цель:** Не терять контекст при возвращении к проекту после перерыва

---

## 📍 БЫСТРЫЙ СТАРТ (5 минут)

### Шаг 1: Определить текущее состояние (1 мин)

**Откройте эти файлы в порядке приоритета:**

1. **`WORKFLOW_STATE.md`** (строки 1-50)
   - Показывает `current_stage`, `current_role`, `overall_status`
   - Показывает последний обновленный этап

2. **`.specify/features/README.md`**
   - Показывает активные фичи (в разработке)
   - Показывает завершенные фичи

3. **`SCENARIO_STATE.yml`**
   - Показывает текущий сценарий (`current_scenario`)
   - Показывает текущую фазу (`phase`)

**Пример чтения:**
```bash
# Быстрая проверка состояния
head -50 WORKFLOW_STATE.md
cat .specify/features/README.md
cat SCENARIO_STATE.yml
```

### Шаг 2: Найти активные задачи (2 мин)

**Для Spec-Driven фич (новые фичи):**

1. Проверьте `.specify/features/{feature-name}/tasks.md`
   - Ищите задачи со статусом `[ ]` (не выполнено)
   - Первая невыполненная задача = ваша следующая задача

2. Проверьте `.specify/features/{feature-name}/spec.md`
   - Убедитесь, что понимаете требования
   - Проверьте `SUCCESS CRITERIA`

**Пример:**
```bash
# Найти активные задачи
find .specify/features -name "tasks.md" -exec grep -l "\[ \]" {} \;

# Посмотреть задачи для video-recording
cat .specify/features/video-recording/tasks.md | grep -A 5 "\[ \]"
```

### Шаг 3: Определить, что делать дальше (2 мин)

**Сценарии:**

#### Сценарий A: Новая фича (Spec-Driven)
```
1. Проверьте этап:
   - spec.md существует? → Перейти к CLARIFY или PLAN
   - clarifications.md существует? → Перейти к PLAN
   - plan.md существует? → Перейти к TASKS
   - tasks.md существует? → Перейти к IMPLEMENT

2. Следующий шаг = первый отсутствующий файл
```

#### Сценарий B: Активная фича (есть tasks.md)
```
1. Откройте tasks.md
2. Найдите первую задачу со статусом [ ]
3. Эта задача = ваша следующая задача
4. Прочитайте связанные файлы (spec.md, plan.md)
```

#### Сценарий C: Нет активных фич
```
1. Проверьте WORKFLOW_STATE.md → раздел "ОТКРЫТЫЕ ВОПРОСЫ"
2. Проверьте .specify/features/README.md → раздел "Планируется"
3. Выберите новую фичу для разработки
```

---

## 🔄 ПОЛНЫЙ WORKFLOW (15 минут)

### 1. Восстановление контекста

#### 1.1. Прочитать состояние системы
```bash
# Основные файлы состояния
cat WORKFLOW_STATE.md | head -100
cat SCENARIO_STATE.yml
cat .specify/features/README.md
```

**Что искать:**
- `current_stage` - текущий этап работы
- `current_role` - текущая роль (если используется LLM-OS)
- `overall_status` - общий статус проекта
- Активные фичи в разработке

#### 1.2. Проверить последние изменения
```bash
# Последние коммиты
git log --oneline -10

# Последние измененные файлы
git diff --name-only HEAD~5..HEAD

# Незакоммиченные изменения
git status
```

#### 1.3. Проверить активные задачи

**Для Spec-Driven фич:**
```bash
# Найти все tasks.md файлы
find .specify/features -name "tasks.md"

# Проверить прогресс задач
for file in $(find .specify/features -name "tasks.md"); do
  echo "=== $file ==="
  grep -c "\[x\]" "$file" || echo "0 completed"
  grep -c "\[ \]" "$file" || echo "0 pending"
done
```

**Для LLM-OS workflow:**
```bash
# Проверить WORKFLOW_STATE.md
grep -A 20 "current_stage" WORKFLOW_STATE.md
```

### 2. Определение следующей задачи

#### 2.1. Если есть активная фича (Spec-Driven)

**Алгоритм:**

1. **Найти фичу в разработке:**
   ```bash
   # Проверить .specify/features/README.md
   cat .specify/features/README.md | grep -A 5 "В процессе"
   ```

2. **Проверить этап фичи:**
   ```bash
   # Для video-recording
   ls -la .specify/features/video-recording/
   # Должны быть: spec.md, clarifications.md, plan.md, tasks.md
   ```

3. **Найти первую невыполненную задачу:**
   ```bash
   # Открыть tasks.md
   cat .specify/features/video-recording/tasks.md
   
   # Найти первую задачу со статусом [ ]
   # Это ваша следующая задача
   ```

4. **Прочитать контекст задачи:**
   ```bash
   # Прочитать spec.md для понимания требований
   cat .specify/features/video-recording/spec.md | head -100
   
   # Прочитать plan.md для понимания архитектуры
   cat .specify/features/video-recording/plan.md | head -100
   ```

#### 2.2. Если нет активной фичи

**Варианты:**

1. **Начать новую фичу:**
   - Проверить `.specify/features/README.md` → раздел "Планируется"
   - Или проверить `docs/requirements/PRD.md` → раздел с новыми фичами
   - Начать с `/specify` команды

2. **Продолжить существующую фичу:**
   - Проверить `WORKFLOW_STATE.md` → раздел "ЭТАПЫ РАБОТЫ"
   - Найти этап со статусом `IN_PROGRESS` или `NOT_STARTED`

3. **Исправить баги:**
   - Проверить Issues в GitHub (если есть)
   - Или проверить `WORKFLOW_STATE.md` → раздел "ОТКРЫТЫЕ ВОПРОСЫ"

### 3. Работа над задачей

#### 3.1. Перед началом работы

**Чек-лист:**

- [ ] Прочитал `spec.md` (если есть)
- [ ] Прочитал `plan.md` (если есть)
- [ ] Прочитал `tasks.md` → понял текущую задачу
- [ ] Прочитал `.specify/constitution.md` → знаю технические стандарты
- [ ] Проверил связанные файлы (если задача их затрагивает)

#### 3.2. Во время работы

**Правила:**

1. **Следовать Spec-Driven workflow:**
   - Если задача из `tasks.md` → реализовать строго по задаче
   - Если нужно отклониться → обновить `tasks.md` или создать уточнение

2. **Обновлять прогресс:**
   - После завершения задачи → обновить `tasks.md`: `[ ]` → `[x]`
   - Добавить комментарий с датой: `@2026-01-06`

3. **Коммитить изменения:**
   ```bash
   # После завершения задачи
   git add .
   git commit -m "feat(video-recording): complete task X.Y - [описание]"
   git push
   ```

#### 3.3. После завершения задачи

**Чек-лист:**

- [ ] Задача выполнена согласно `tasks.md`
- [ ] Код соответствует `.specify/constitution.md`
- [ ] Обновлен `tasks.md` (статус задачи)
- [ ] Изменения закоммичены
- [ ] Если это последняя задача этапа → обновить `WORKFLOW_STATE.md`

---

## 📋 ЧЕК-ЛИСТ ДЛЯ БЫСТРОГО ВОЗВРАЩЕНИЯ

### ⏱️ 5-минутный чек-лист

```markdown
- [ ] Прочитал WORKFLOW_STATE.md (строки 1-50)
- [ ] Прочитал .specify/features/README.md
- [ ] Определил активную фичу (если есть)
- [ ] Нашел первую невыполненную задачу в tasks.md
- [ ] Готов начать работу
```

### ⏱️ 15-минутный чек-лист

```markdown
- [ ] Прочитал WORKFLOW_STATE.md (полностью)
- [ ] Прочитал SCENARIO_STATE.yml
- [ ] Прочитал .specify/features/README.md
- [ ] Проверил последние коммиты (git log -10)
- [ ] Определил активную фичу
- [ ] Прочитал spec.md активной фичи
- [ ] Прочитал plan.md активной фичи (если есть)
- [ ] Прочитал tasks.md → нашел текущую задачу
- [ ] Прочитал .specify/constitution.md (если нужно)
- [ ] Готов начать работу
```

---

## 🎯 ПРИМЕРЫ РАБОЧИХ СЦЕНАРИЕВ

### Сценарий 1: Возвращение к проекту после недели

**Ситуация:** Не работали над проектом неделю, нужно быстро восстановить контекст.

**Действия:**

1. **Быстрая проверка (2 мин):**
   ```bash
   head -50 WORKFLOW_STATE.md
   cat .specify/features/README.md
   ```

2. **Проверка активных задач (2 мин):**
   ```bash
   cat .specify/features/video-recording/tasks.md | grep -A 3 "\[ \]"
   ```

3. **Восстановление контекста (5 мин):**
   ```bash
   # Прочитать spec.md для понимания фичи
   cat .specify/features/video-recording/spec.md | head -100
   
   # Прочитать последние изменения
   git log --oneline -5
   ```

4. **Начать работу:**
   - Первая невыполненная задача из `tasks.md` = ваша задача

### Сценарий 2: Новая сессия работы (30 мин)

**Ситуация:** Есть 30 минут свободного времени, хотите продолжить работу.

**Действия:**

1. **Восстановление контекста (5 мин):**
   - Прочитать `WORKFLOW_STATE.md`
   - Прочитать `.specify/features/README.md`
   - Проверить последние коммиты

2. **Определение задачи (5 мин):**
   - Найти активную фичу
   - Найти первую невыполненную задачу
   - Прочитать контекст задачи (spec.md, plan.md)

3. **Работа над задачей (15 мин):**
   - Реализовать задачу
   - Обновить `tasks.md`
   - Закоммитить изменения

4. **Подготовка к следующей сессии (5 мин):**
   - Обновить `WORKFLOW_STATE.md` (если нужно)
   - Закоммитить все изменения
   - Записать заметки о следующей задаче

### Сценарий 3: Начало новой фичи

**Ситуация:** Текущая фича завершена, нужно начать новую.

**Действия:**

1. **Выбор фичи (5 мин):**
   ```bash
   # Проверить планируемые фичи
   cat .specify/features/README.md | grep -A 10 "Планируется"
   
   # Или проверить PRD
   cat docs/requirements/PRD.md | grep -A 5 "Core Features"
   ```

2. **Создание спецификации (10 мин):**
   ```
   /specify
   
   Feature: {название фичи}
   
   REQUIREMENTS:
   {требования}
   ```

3. **Продолжить Spec-Driven цикл:**
   - CLARIFY → PLAN → TASKS → IMPLEMENT

---

## 🔍 ПОЛЕЗНЫЕ КОМАНДЫ

### Быстрая проверка состояния

```bash
# Текущий статус проекта
head -20 WORKFLOW_STATE.md

# Активные фичи
cat .specify/features/README.md

# Последние коммиты
git log --oneline -10

# Незакоммиченные изменения
git status
```

### Поиск активных задач

```bash
# Найти все tasks.md файлы
find .specify/features -name "tasks.md"

# Показать прогресс задач
for file in $(find .specify/features -name "tasks.md"); do
  echo "=== $file ==="
  completed=$(grep -c "\[x\]" "$file" 2>/dev/null || echo "0")
  pending=$(grep -c "\[ \]" "$file" 2>/dev/null || echo "0")
  echo "Completed: $completed, Pending: $pending"
done
```

### Восстановление контекста фичи

```bash
# Для video-recording
FEATURE="video-recording"

# Показать структуру фичи
ls -la .specify/features/$FEATURE/

# Показать прогресс
echo "=== SPEC ==="
head -50 .specify/features/$FEATURE/spec.md

echo "=== TASKS ==="
grep -A 3 "\[ \]" .specify/features/$FEATURE/tasks.md | head -20
```

---

## 📝 ШАБЛОН ДЛЯ ЗАМЕТОК

**Создайте файл `NOTES.md` в корне проекта для личных заметок:**

```markdown
# Мои заметки

## 2026-01-06

### Текущая задача
- Фича: Video Recording
- Задача: Task 4.1 - Implement BeforeUnload Warning
- Статус: В работе

### Что сделано
- Реализован beforeunload handler
- Добавлены проверки для recording и upload состояний

### Что дальше
- Протестировать на разных браузерах
- Обновить tasks.md после завершения

### Вопросы/Блокеры
- Нет
```

---

## 🎯 КЛЮЧЕВЫЕ ПРИНЦИПЫ

1. **Всегда начинайте с чтения состояния:**
   - `WORKFLOW_STATE.md` → общее состояние
   - `.specify/features/README.md` → активные фичи
   - `tasks.md` → конкретные задачи

2. **Одна задача за раз:**
   - Не пытайтесь сделать все сразу
   - Завершите одну задачу → обновите `tasks.md` → коммит

3. **Документируйте прогресс:**
   - Обновляйте `tasks.md` после каждой задачи
   - Коммитьте изменения регулярно
   - Обновляйте `WORKFLOW_STATE.md` при завершении этапа

4. **Не теряйте контекст:**
   - Все важное в файлах (не в памяти)
   - Используйте `NOTES.md` для личных заметок
   - Коммитьте часто, чтобы не потерять прогресс

---

## 🔗 СВЯЗАННЫЕ ДОКУМЕНТЫ

- `docs/planning/spec_driven_workflow_guide.md` - Полный workflow для всех стадий
- `PROJECT_CONFIG.md` - Правила LLM-OS системы
- `.specify/constitution.md` - Технические стандарты
- `WORKFLOW_STATE.md` - Текущее состояние проекта

---

**Последнее обновление:** 2026-01-06



