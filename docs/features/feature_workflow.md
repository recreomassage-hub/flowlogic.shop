# 🚀 FEATURE WORKFLOW - Канонический процесс разработки фичи

**Дата создания:** 2025-12-26  
**Цель:** Production-ready процесс разработки фич с ИИ

---

## 🎯 Что такое "фича" в LLM-OS

**Фича = минимально завершённая бизнес-ценность, которую можно:**
- ✅ Включить
- ✅ Протестировать
- ✅ Откатить
- ✅ Объяснить

---

## 🧩 ОБЩИЙ СЦЕНАРИЙ (HIGH-LEVEL)

```
IDEA
 ↓
FEATURE BRIEF          (обязательный старт)
 ↓
IMPACT ANALYSIS        (если HIGH → разбить)
 ↓
DESIGN (API / UX)      (только под эту фичу)
 ↓
IMPLEMENTATION         (микро-циклы: slice → commit)
 ↓
QA                     (не в конце!)
 ↓
RELEASE / TOGGLE       (feature flag)
 ↓
RETROSPECTIVE          (обучение для следующих фич)
```

**Каждый шаг = отдельный микро-этап, а не "всё сразу"**

---

## 🧠 СЦЕНАРИЙ 15: FEATURE BRIEF (ОБЯЗАТЕЛЬНЫЙ СТАРТ)

**Когда:** Перед тем как ИИ напишет хотя бы одну строку кода

**Делает роль:** PM (или OWNER)

**Команда:**
```bash
./llmos feature new <feature_name> [description]
# или
./scripts/feature_new.sh <feature_name> [description]
```

**Создает:**
- `docs/features/<slug>/feature_brief.md` - Описание фичи
- `docs/features/<slug>/impact_analysis.md` - Шаблон анализа
- `docs/features/<slug>/design/` - Шаблоны дизайна
- `docs/features/<slug>/qa.md` - Шаблон QA
- `docs/features/<slug>/retro.md` - Шаблон ретроспективы

**📌 Без этого файла фича не стартует.**

**Пример feature_brief.md:**
```markdown
# Feature: Smart Task Prioritization

## 🎯 Цель
Помочь пользователю видеть самые важные задачи первыми

## 👤 Пользователь
- Role: knowledge worker
- Pain: слишком много задач, теряется фокус

## ✅ Success Criteria
- Пользователь тратит <5 сек на понимание приоритета
- ≥60% задач открываются по рекомендованному порядку

## 🚫 Out of Scope
- ML-обучение
- командная приоритизация

## 🔁 Rollback
Feature flag: smart_priority=false
```

---

## 🔎 СЦЕНАРИЙ 16: FEATURE IMPACT ANALYSIS

**Делает:** ARCHITECT + PM

**Команда:**
```bash
./llmos feature impact <feature_slug>
# или
./scripts/feature_impact.sh <feature_slug>
```

**В Cursor:**
```
ARCHITECT:
Проанализируй влияние фичи:
1. Backend
2. Frontend
3. Data model
4. Performance
5. Security
Выдай таблицу impact / risk / mitigation
```

**Результат →** `docs/features/<slug>/impact_analysis.md`

**📌 Если impact HIGH → фича дробится.**

---

## 🎨 СЦЕНАРИЙ 17: FEATURE DESIGN (ТОЛЬКО ТО, ЧТО НУЖНО)

**Делают:**
- ARCHITECT — API
- FRONTEND — UX
- BACKEND — data

**Правило:**
- ❌ Никакой "идеальной архитектуры"
- ✅ Только под эту фичу

**Артефакты:**
```
docs/features/<slug>/
├── design/
│   ├── api.md
│   ├── ux.md
│   └── data.md
```

**В Cursor (пример):**
```
BACKEND_DEV:
Спроектируй минимальный API для фичи.
Условия:
- backward compatible
- no breaking changes
- feature-flag ready
```

---

## 🛠️ СЦЕНАРИЙ 18: IMPLEMENTATION LOOP (МИКРО-ЦИКЛ)

**Главная ошибка:** ИИ пишет всю фичу целиком.

**Правильный цикл:**
```
Design slice
 → Implement
 → Commit
 → Validate
```

**🔁 Цикл:**
```bash
./step.sh   # после каждого slice
```

**В Cursor:**
```
IMPLEMENT ONLY:
- один endpoint
- один UI компонент
- одна миграция
После — обнови WORKFLOW_STATE.md
```

**📌 5–10 коммитов на фичу — норма.**

---

## 🧪 СЦЕНАРИЙ 19: FEATURE-QA (НЕ В КОНЦЕ!)

**Делает:** QA + DEV

**Файл:** `docs/features/<slug>/qa.md`

**Структура:**
```markdown
## Positive cases
- [ ] Приоритет виден
- [ ] Сортировка корректна

## Negative cases
- [ ] Нет задач
- [ ] Отключен feature flag

## Regression
- [ ] Старые фичи не сломаны
```

**В Cursor:**
```
QA:
Сгенерируй edge cases для этой фичи.
Только реальные сценарии пользователя.
```

---

## 🚦 СЦЕНАРИЙ 20: FEATURE FLAG + RELEASE

**Обязательное правило:** Каждая фича = флаг

**Конфигурация:**
```yaml
features:
  smart_priority: false
```

**Проверка флага:**
```bash
./llmos feature check-flag <feature_slug>
# или
./scripts/feature_check_flag.sh <feature_slug>
```

**Release сценарий:**
```bash
./step.sh
git tag feature-<slug>
```

**В Cursor:**
```
PM:
Опиши как включить/выключить фичу.
И какие метрики смотреть первые 24 часа.
```

---

## 🔄 СЦЕНАРИЙ 21: FEATURE ROLLBACK (ОБЯЗАТЕЛЬНО)

**Если метрики плохие:**
```bash
# config
smart_priority=false

git revert <feature_commit>
```

**📌 Фича без rollback — бомба замедленного действия.**

---

## 🧠 СЦЕНАРИЙ 22: FEATURE RETROSPECTIVE (ТОП-1 ПРОПУСКАЮТ)

**Делает:** OWNER + LLM

**Команда:**
```bash
./llmos feature retro <feature_slug>
# или
./scripts/feature_retro.sh <feature_slug>
```

**Файл:** `docs/features/<slug>/retro.md`

**Структура:**
```markdown
## Что сработало
- быстрый дизайн
- малые коммиты

## Что нет
- недооценили UX

## Вывод
Следующие фичи начинать с UX
```

**📌 Это обучает следующие фичи, а не только эту.**

---

## 🧩 КОРОТКАЯ ШПАРГАЛКА: FEATURE-FLOW

1. ✅ **Feature brief** (PM) - `./llmos feature new`
2. ✅ **Impact analysis** (ARCHITECT) - `./llmos feature impact`
3. ✅ **Minimal design** (DEV) - Design templates
4. ✅ **Micro-implementation** (step.sh × N) - Малые коммиты
5. ✅ **QA before release** (QA) - QA template
6. ✅ **Feature flag + release** - `./llmos feature check-flag`
7. ✅ **Rollback ready** - Feature flag off
8. ✅ **Retro** - `./llmos feature retro`

---

## 🎯 КОМАНДЫ LLMOS

```bash
# Создать новую фичу
./llmos feature new <feature_name> [description]

# Анализ влияния
./llmos feature impact <feature_slug>

# Проверить feature flag
./llmos feature check-flag <feature_slug>

# Ретроспектива
./llmos feature retro <feature_slug>

# Список фич
./llmos feature list
```

---

## 📁 СТРУКТУРА ФИЧИ

```
docs/features/<slug>/
├── feature_brief.md      # Обязательно перед стартом
├── impact_analysis.md   # Анализ влияния
├── design/
│   ├── api.md           # API дизайн
│   ├── ux.md            # UX дизайн
│   └── data.md          # Data model
├── qa.md                # Тестирование
├── retro.md             # Ретроспектива
└── README.md            # Обзор фичи
```

---

## ⚠️ ВАЖНЫЕ ПРАВИЛА

1. **Без Feature Brief — фича не стартует**
2. **Если Impact HIGH — фича дробится**
3. **Микро-коммиты (5-10 на фичу) — норма**
4. **QA не в конце, а во время разработки**
5. **Каждая фича = feature flag**
6. **Rollback обязателен**
7. **Retro обязателен (обучение для следующих фич)**

---

## 📚 ДОПОЛНИТЕЛЬНЫЕ РЕСУРСЫ

- **Скрипты:** `scripts/feature_*.sh`
- **Примеры:** `docs/features/*/`
- **Процессные сценарии:** `docs/process_scenarios.md`

---

**Последнее обновление:** 2025-12-26


