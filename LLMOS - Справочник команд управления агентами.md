# 🤖 LLMOS: Справочник команд управления агентами

> [!INFO] Описание системы
> 
> Система управления жизненным циклом разработки через 40 специализированных промптов в директории PROMPTS/. Автоматизирует переходы между ролями и этапами проектирования.

---

## 🚀 Основные команды

| **Команда**       | **Описание**       | **Действие**                                                      |
| ----------------- | ------------------ | ----------------------------------------------------------------- |
| `./llmos tz-full` | **TZ Pipeline**    | Запускает полный цикл: `TZ Analyst` → `TZ Reviewer` → `APPROVED`  |
| `./llmos next`    | **Smart Next**     | Авто-определение следующего шага на основе `WORKFLOW_STATE.md`    |
| `./llmos status`  | **System Status**  | Выводит: текущий этап, активную роль, следующий промпт и прогресс |
| `./llmos approve` | **Final Approval** | Финальное одобрение проекта (права OWNER)                         |
| `./llmos commit`  | **Atomic Commit**  | Запуск `./step.sh` для фиксации изменений текущего шага           |
| `./llmos help`    | **Help**           | Вывод краткой справки по всем командам                            |

---

## 🎭 Работа с ролями (Execution & Review)

### Режимы выполнения

- `./llmos execute [ROLE]` — Запуск **EXECUTE** режима. ИИ берет задачу в работу.
    
- `./llmos self [ROLE]` — Запуск **SELF-REVIEW**. Агент сам проверяет свои артефакты на ошибки.
    
- `./llmos peer [ROLE]` — Запуск **PEER-REVIEW**. Кросс-проверка (например, ARCHITECT проверяет за ANALYST).
    

### Список поддерживаемых ролей

- [ ] `ANALYST` (Аналитик)
    
- [ ] `ARCHITECT` (Архитектор)
    
- [ ] `PM` (Менеджер проекта)
    
- [ ] `BACKEND_DEV` (Бэкенд)
    
- [ ] `FRONTEND_DEV` (Фронтенд)
    
- [ ] `INFRA_DEVOPS` (Инфраструктура)
    
- [ ] `QA` (Тестирование)
    
- [ ] `SECURITY` (Безопасность)
    
- [ ] `DOCS` (Документация)
    
- [ ] `OWNER` (Владелец/Контроль)
    

---

## 🛠 Примеры использования (Workflow)

> [!EXAMPLE] Типовой рабочий процесс
> 
> 1. **Старт проекта:**
>     
>     Bash
>     
>     ```
>     ./llmos tz-full
>     ```
>     
> 2. **Разработка (Backend):**
>     
>     Bash
>     
>     ```
>     ./llmos execute BACKEND_DEV
>     ./llmos self BACKEND_DEV
>     ./llmos commit
>     ```
>     
> 3. **Переход к следующему шагу:**
>     
>     Bash
>     
>     ```
>     ./llmos next
>     ```
>     
> 4. **Проверка состояния:**
>     
>     Bash
>     
>     ```
>     ./llmos status
>     ```
>     

---

## 📁 Структура данных

- **Контекст:** `WORKFLOW_STATE.md` (хранит состояние системы).
    
- **Инструкции:** `PROMPTS/` (содержит 40 файлов промптов).
    
- **Скрипт коммитов:** `./step.sh`.
    

#LLMOS #Workflow #Cursor #AI #Automation