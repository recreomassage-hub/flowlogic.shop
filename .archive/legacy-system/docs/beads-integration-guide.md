# Руководство по интеграции Beads

**Дата:** 2026-01-09  
**Цель:** Интегрировать официальный Beads с существующей системой

---

## 🎯 Варианты интеграции

### Вариант 1: Полная замена `bd.sh` на Beads ⭐ (Рекомендуется)

**Когда использовать:** Если Beads полностью покрывает функциональность `bd.sh` и формат данных совместим.

**Шаги:**

1. **Установить Beads:**
   ```bash
   ./scripts/install-beads.sh
   # Или вручную после нахождения репозитория
   ```

2. **Мигрировать данные:**
   ```bash
   # Создать скрипт миграции из .beads/issues.jsonl в формат Beads
   ./scripts/migrate-to-beads.sh
   ```

3. **Обновить алиасы:**
   ```bash
   # В .aliases заменить _bd на beads
   alias bd="beads"
   alias bd-ready="beads ready"
   # и т.д.
   ```

4. **Обновить скрипты:**
   - `scripts/generate-status.sh` - использовать `beads` вместо `bd.sh`
   - Все команды `/create-issues` - использовать `beads create`

5. **Тестирование:**
   ```bash
   beads init
   beads ready
   beads start DS-1
   ```

**Плюсы:**
- ✅ Официальный инструмент с поддержкой
- ✅ Больше функций (MCP, интеграции)
- ✅ Активная разработка

**Минусы:**
- ❌ Нужна миграция данных
- ❌ Нужно обновить все скрипты
- ❌ Возможна несовместимость

---

### Вариант 2: Параллельное использование

**Когда использовать:** Для тестирования Beads без риска сломать текущую систему.

**Шаги:**

1. **Установить Beads:**
   ```bash
   ./scripts/install-beads.sh
   ```

2. **Использовать Beads для новых проектов:**
   ```bash
   # Новый проект
   mkdir new-project
   cd new-project
   beads init
   beads create "New task"
   ```

3. **Оставить `bd.sh` для текущих задач:**
   ```bash
   # В текущем проекте продолжаем использовать bd.sh
   bd ready
   bd start DS-1
   ```

4. **Постепенная миграция:**
   - Тестировать Beads на новых эпиках
   - Сравнивать функциональность
   - Мигрировать по готовности

**Плюсы:**
- ✅ Безопасное тестирование
- ✅ Нет риска сломать текущую систему
- ✅ Можно сравнить оба инструмента

**Минусы:**
- ❌ Дублирование данных
- ❌ Нужно поддерживать оба инструмента

---

### Вариант 3: Обертка для совместимости

**Когда использовать:** Если нужно сохранить обратную совместимость и плавно мигрировать.

**Шаги:**

1. **Установить Beads:**
   ```bash
   ./scripts/install-beads.sh
   ```

2. **Создать обертку `bd` в `.aliases`:**
   ```bash
   # Умная обертка - использует beads если установлен, иначе bd.sh
   bd() {
       if command -v beads &> /dev/null; then
           beads "$@"
       else
           "$PROJECT_ROOT/scripts/bd.sh" "$@"
       fi
   }
   ```

3. **Постепенная миграция:**
   - Команды автоматически используют Beads если установлен
   - Можно откатиться к `bd.sh` в любой момент
   - Миграция данных по мере необходимости

**Плюсы:**
- ✅ Полная обратная совместимость
- ✅ Плавная миграция
- ✅ Можно использовать оба инструмента

**Минусы:**
- ❌ Нужно поддерживать обертку
- ❌ Возможны различия в командах

---

## 🔧 Технические детали

### Миграция данных

**Формат `.beads/issues.jsonl`:**
```json
{"id":"DS-1","title":"Task","status":"ready","priority":1,"epic":"design-system","blocked_by":[],...}
{"id":"DS-2","title":"Task 2","status":"ready","priority":1,"epic":"design-system","blocked_by":["DS-1"],...}
```

**Нужно проверить:**
1. Какой формат использует Beads?
2. Можно ли импортировать JSONL?
3. Совместимы ли поля (`id`, `status`, `blocked_by`, и т.д.)?

**Скрипт миграции (пример):**
```bash
#!/bin/bash
# migrate-to-beads.sh

# Читаем .beads/issues.jsonl и конвертируем в формат Beads
cat .beads/issues.jsonl | while IFS= read -r line; do
    id=$(echo "$line" | jq -r '.id')
    title=$(echo "$line" | jq -r '.title')
    epic=$(echo "$line" | jq -r '.epic')
    priority=$(echo "$line" | jq -r '.priority')
    
    # Создаем задачу в Beads
    beads create "$title" --epic "$epic" --priority "$priority"
done
```

### Обновление алиасов

**Текущие алиасы в `.aliases`:**
```bash
_bd() {
    "$PROJECT_ROOT/scripts/bd.sh" "$@"
}
```

**После миграции на Beads:**
```bash
_bd() {
    beads "$@"
}
```

Или использовать обертку:
```bash
_bd() {
    if command -v beads &> /dev/null; then
        beads "$@"
    else
        "$PROJECT_ROOT/scripts/bd.sh" "$@"
    fi
}
```

### Обновление скриптов

**Файлы для обновления:**
1. `scripts/generate-status.sh` - заменить `./scripts/bd.sh` на `beads`
2. `.aliases` - обновить функции
3. `CHEATSHEET.md` - обновить примеры команд
4. `примеры-мультиагентс-воркфлоу.md` - обновить примеры

---

## 📋 Чеклист миграции

### Подготовка
- [ ] Найти официальный репозиторий Beads
- [ ] Установить Go (если нужно)
- [ ] Установить Beads
- [ ] Изучить команды Beads (`beads --help`)
- [ ] Проверить формат данных Beads

### Тестирование
- [ ] Протестировать Beads на тестовом проекте
- [ ] Сравнить функциональность с `bd.sh`
- [ ] Проверить совместимость форматов
- [ ] Протестировать миграцию данных

### Миграция
- [ ] Создать скрипт миграции данных
- [ ] Сделать бэкап `.beads/issues.jsonl`
- [ ] Выполнить миграцию
- [ ] Проверить корректность данных
- [ ] Обновить алиасы
- [ ] Обновить скрипты
- [ ] Обновить документацию

### Проверка
- [ ] Все команды работают
- [ ] Данные корректны
- [ ] Интеграция с Spec-Driven работает
- [ ] STATUS.md генерируется правильно

---

## 🚀 Быстрый старт

### 1. Установка

```bash
# Установить Go (если нужно)
sudo apt install golang-go

# Установить Beads
./scripts/install-beads.sh

# Проверить установку
beads --version
```

### 2. Инициализация

```bash
# В корне проекта
beads init

# Или использовать существующий .beads/
# (нужно проверить совместимость)
```

### 3. Тестирование

```bash
# Создать тестовую задачу
beads create "Test task" --priority 1

# Показать готовые задачи
beads ready

# Начать работу
beads start <task-id>

# Завершить
beads complete <task-id>
```

---

## ⚠️ Важные замечания

1. **Бэкап данных:**
   ```bash
   cp -r .beads .beads.backup
   git add .beads.backup
   git commit -m "backup: перед миграцией на Beads"
   ```

2. **Тестирование:**
   - Сначала тестировать на копии проекта
   - Проверить все команды
   - Убедиться в корректности данных

3. **Откат:**
   - Сохранить текущий `bd.sh`
   - Сохранить `.beads/issues.jsonl`
   - Можно откатиться в любой момент

---

## 🔗 Ресурсы

- [Статья на Habr](https://habr.com/ru/articles/983500/)
- GitHub репозиторий: **TBD** (нужно найти)
- Документация Beads: **TBD**

---

**Статус:** Готов к установке после нахождения репозитория

