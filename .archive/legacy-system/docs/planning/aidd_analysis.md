# Анализ AI-Driven Development (AIDD) vs Существующая система

**Дата:** 2026-01-06  
**Цель:** Сравнение AIDD методологии с существующей LLM-OS + Spec-Driven системой

---

## 🔍 ПРОТИВОРЕЧИЯ С СУЩЕСТВУЮЩЕЙ СИСТЕМОЙ

### 1. Дублирование структуры артефактов

**AIDD предлагает:**
```
docs/prd/T-104.prd.md
docs/plan/T-104.md
docs/tasklist/T-104.md
docs/research/T-104.md
reports/qa/T-104.md
```

**Существующая система:**
```
.specify/features/{name}/
  ├── spec.md              # = PRD
  ├── clarifications.md
  ├── plan.md              # = plan
  ├── tasks.md             # = tasklist
  └── testing_checklist.md # = QA report
```

**Противоречие:** Две параллельные структуры для одних и тех же артефактов.

**Решение:** Использовать существующую структуру `.specify/features/`, добавить `research.md` при необходимости.

---

### 2. Дублирование ролей

**AIDD предлагает:**
- analyst, researcher, planner, implementer, reviewer, qa, validator, tech-writer, task-planner

**Существующая система:**
- ANALYST, ARCHITECT, PM, BACKEND_DEV, FRONTEND_DEV, QA, SECURITY, DOCS, OWNER

**Противоречие:** 
- `researcher` = часть ANALYST
- `planner` = PM
- `implementer` = BACKEND_DEV + FRONTEND_DEV
- `reviewer` = PEER-REVIEW (уже есть в системе)
- `validator` = QA + SECURITY
- `tech-writer` = DOCS
- `task-planner` = PM

**Решение:** Использовать существующие роли, расширить их инструкции при необходимости.

---

### 3. Дублирование команд

**AIDD предлагает:**
```
/idea → analyst
/plan → planner
/tasks → task-planner
/qa → qa
/validate → validator
/docs-update → tech-writer
```

**Существующая система:**
```
/specify → создает spec.md
/clarify → создает clarifications.md
/plan → создает plan.md
/tasks → создает tasks.md
/implement → реализует задачу
```

**Противоречие:** 
- `/idea` не существует в системе (как выяснили ранее)
- `/plan`, `/tasks` уже есть, но работают через Spec-Driven workflow
- `/qa`, `/validate`, `/docs-update` отсутствуют

**Решение:** 
- Добавить `/idea` как алиас для `/specify` (начало новой фичи)
- Расширить существующие команды для поддержки QA/validation
- Добавить `/qa` и `/validate` команды

---

### 4. Дублирование agreements

**AIDD предлагает:**
```
conventions.md    # общие конвенции
CLAUDE.md         # инструкция для Claude
workflow.md       # описание процесса
```

**Существующая система:**
```
.specify/constitution.md  # технические стандарты (≈ conventions.md)
.cursorrules             # правила для AI (≈ CLAUDE.md)
PROJECT_CONFIG.md        # архитектура системы (≈ workflow.md)
docs/planning/spec_driven_workflow_guide.md  # описание процесса
```

**Противоречие:** Полное дублирование функциональности.

**Решение:** Использовать существующие файлы:
- `.specify/constitution.md` = conventions.md
- `.cursorrules` = CLAUDE.md
- `PROJECT_CONFIG.md` + `spec_driven_workflow_guide.md` = workflow.md

---

### 5. Quality Gates

**AIDD предлагает:**
```
AGREEMENTS_ON → PRD_READY → PLAN_APPROVED → TASKLIST_READY → 
IMPLEMENT_STEP_OK → REVIEW_OK → RELEASE_READY → DOCS_UPDATED
```

**Существующая система:**
```
EXECUTE → PEER-REVIEW → APPROVED/NEEDS_REVISION/BLOCKED
```

**Противоречие:** 
- AIDD gates более детализированы
- Существующая система использует PEER-REVIEW как основной gate

**Решение:** Интегрировать AIDD gates как дополнительные проверки внутри PEER-REVIEW этапов.

---

## ✅ ВОЗМОЖНЫЕ УЛУЧШЕНИЯ (БЕЗ ПРОТИВОРЕЧИЙ)

### 1. Добавить команду `/idea`

**Текущее состояние:** Команда не существует.

**Предложение:**
- `/idea` → алиас для `/specify` с фокусом на начальную идею
- Создает `spec.md` с базовой структурой
- Может быть расширена для создания feature brief (как в feature_workflow.md)

**Интеграция:**
```markdown
# В .cursorrules добавить:
commands:
  idea: "Начать новую фичу: @.cursor/commands/specify.md (с фокусом на идею)"
```

---

### 2. Добавить `/qa` и `/validate` команды

**Текущее состояние:** QA роль есть, но нет команды для запуска.

**Предложение:**
- `/qa` → запускает QA роль для текущей фичи
- `/validate` → проверяет quality gates перед релизом

**Интеграция:**
```markdown
# В .cursorrules добавить:
commands:
  qa: "Запустить QA проверку: @.cursor/commands/qa.md"
  validate: "Проверить quality gates: @.cursor/commands/validate.md"
```

---

### 3. Добавить `research.md` в структуру фичи

**Текущее состояние:** Нет отдельного файла для исследования кодовой базы.

**Предложение:**
- Добавить `.specify/features/{name}/research.md` для отчетов по исследованию
- Использовать при сложных интеграциях

**Интеграция:**
- Расширить ANALYST роль для создания research.md
- Добавить в workflow: SPECIFY → RESEARCH (опционально) → CLARIFY → PLAN

---

### 4. Улучшить Quality Gates

**Текущее состояние:** PEER-REVIEW как основной gate.

**Предложение:**
- Добавить явные статусы в `tasks.md`: `[ ]` → `[in-progress]` → `[x]`
- Добавить проверку gates перед переходом к следующему этапу
- Интегрировать в PEER-REVIEW процесс

**Интеграция:**
```markdown
# В tasks.md добавить:
## Quality Gates
- [ ] PRD_READY: spec.md заполнен, нет блокирующих вопросов
- [ ] PLAN_APPROVED: plan.md создан, архитектура описана
- [ ] TASKLIST_READY: все задачи разбиты, есть acceptance criteria
```

---

## 🗑️ ИЗБЫТОЧНЫЕ ДОКУМЕНТЫ И ПРАВИЛА

### 1. Дублирование workflow описаний

**Избыточность:**
- `docs/features/feature_workflow.md` (IDEA → FEATURE BRIEF → ...)
- `docs/planning/spec_driven_workflow_guide.md` (SPECIFY → CLARIFY → ...)
- `PROJECT_CONFIG.md` (PLAN/BUILD подход)

**Проблема:** Три разных описания одного процесса.

**Решение:** 
- Объединить в один документ: `spec_driven_workflow_guide.md`
- Удалить дублирующие разделы из `feature_workflow.md`
- Оставить `PROJECT_CONFIG.md` только для архитектуры LLM-OS

---

### 2. Дублирование ролей в разных форматах

**Избыточность:**
- `ROLES/*.md` (инструкции для ролей)
- `PROMPTS/*.md` (промпты для ролей)
- Предложение AIDD: `.claude/agents/*.md`

**Проблема:** Три формата для одних и тех же ролей.

**Решение:**
- Использовать `ROLES/*.md` как основной формат
- `PROMPTS/*.md` можно оставить для legacy или удалить
- НЕ создавать `.claude/agents/*.md` (дублирование)

---

### 3. Избыточные команды

**Избыточность:**
- AIDD предлагает `/idea`, `/plan`, `/tasks` (уже есть как `/specify`, `/plan`, `/tasks`)
- AIDD предлагает `/researcher` (можно добавить как опцию в `/specify`)

**Решение:**
- Использовать существующие команды
- Добавить `/idea` как алиас для `/specify`
- НЕ создавать дублирующие команды

---

### 4. Дублирование agreements

**Избыточность:**
- `.specify/constitution.md` (технические стандарты)
- `.cursorrules` (правила для AI)
- AIDD предлагает: `conventions.md`, `CLAUDE.md`, `workflow.md`

**Решение:**
- Использовать существующие файлы
- НЕ создавать дублирующие agreements
- При необходимости расширить существующие файлы

---

## 📊 СРАВНИТЕЛЬНАЯ ТАБЛИЦА

| Элемент AIDD | Существующий аналог | Статус |
|--------------|---------------------|--------|
| `conventions.md` | `.specify/constitution.md` | ✅ Есть |
| `CLAUDE.md` | `.cursorrules` | ✅ Есть |
| `workflow.md` | `spec_driven_workflow_guide.md` | ✅ Есть |
| `docs/prd/*.md` | `.specify/features/*/spec.md` | ✅ Есть |
| `docs/plan/*.md` | `.specify/features/*/plan.md` | ✅ Есть |
| `docs/tasklist/*.md` | `.specify/features/*/tasks.md` | ✅ Есть |
| `docs/research/*.md` | ❌ Нет | ⚠️ Можно добавить |
| `/idea` | ❌ Нет | ⚠️ Можно добавить |
| `/qa` | ❌ Нет | ⚠️ Можно добавить |
| `/validate` | ❌ Нет | ⚠️ Можно добавить |
| `.claude/agents/*` | `ROLES/*.md` | ❌ Дублирование |
| `.claude/commands/*` | `.cursor/commands/*` | ✅ Есть |
| Quality Gates | PEER-REVIEW | ⚠️ Можно улучшить |

---

## 🎯 РЕКОМЕНДАЦИИ ПО ИНТЕГРАЦИИ

### Минимальные изменения (без противоречий)

1. **Добавить `/idea` команду:**
   - Алиас для `/specify` с фокусом на начальную идею
   - Создает базовую структуру `spec.md`

2. **Добавить `/qa` и `/validate` команды:**
   - `/qa` → запускает QA проверку текущей фичи
   - `/validate` → проверяет quality gates

3. **Добавить `research.md` (опционально):**
   - Для сложных интеграций
   - Создается ANALYST ролью при необходимости

4. **Улучшить Quality Gates:**
   - Добавить явные статусы в `tasks.md`
   - Интегрировать проверку gates в PEER-REVIEW

### НЕ делать

1. ❌ НЕ создавать дублирующие структуры (`docs/prd/`, `docs/plan/`, `docs/tasklist/`)
2. ❌ НЕ создавать `.claude/agents/*` (дублирование `ROLES/*.md`)
3. ❌ НЕ создавать `conventions.md`, `CLAUDE.md`, `workflow.md` (уже есть аналоги)
4. ❌ НЕ создавать новые роли (использовать существующие)

---

## 📋 ИТОГОВЫЙ ВЫВОД

**Существующая система уже реализует 90% функциональности AIDD:**

✅ Agreements (constitution.md, .cursorrules)  
✅ Артефакты (spec.md, plan.md, tasks.md)  
✅ Роли (ANALYST, ARCHITECT, PM, QA, etc.)  
✅ Команды (/specify, /plan, /tasks, /implement)  
✅ Quality Gates (PEER-REVIEW)  

**Что можно добавить из AIDD:**

1. `/idea` команда (алиас для `/specify`)
2. `/qa` и `/validate` команды
3. `research.md` для сложных интеграций
4. Более детализированные Quality Gates

**Что НЕ нужно добавлять:**

1. Дублирующие структуры артефактов
2. Дублирующие роли
3. Дублирующие agreements
4. Дублирующие команды

**Рекомендация:** Использовать существующую систему как основу, добавить только недостающие элементы из AIDD (команды `/idea`, `/qa`, `/validate` и опциональный `research.md`).

---

**Последнее обновление:** 2026-01-06

