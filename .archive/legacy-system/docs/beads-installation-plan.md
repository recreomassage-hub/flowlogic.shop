# План установки и интеграции Beads

**Дата:** 2026-01-09  
**Цель:** Установить официальный Beads и интегрировать с существующей системой

---

## 🔍 Текущая ситуация

- ✅ Используется собственная реализация `bd.sh` (Bash + jq)
- ✅ Работает с `.beads/issues.jsonl` (JSONL формат)
- ✅ Интегрирован с Spec-Driven workflow
- ✅ Есть алиасы в `.aliases`
- ❌ Официальный Beads не установлен

---

## 📋 План установки Beads

### Шаг 1: Поиск официального репозитория

**Проблема:** Официальный репозиторий Beads не указан в статье. Нужно найти:

1. GitHub репозиторий (вероятно `beads-dev/beads` или похожий)
2. Инструкции по установке
3. Формат данных (совместим ли с нашим `.beads/issues.jsonl`)

### Шаг 2: Установка Go (если нужно)

```bash
# Проверка наличия Go
go version

# Если нет, установка (Ubuntu/Debian)
sudo apt update
sudo apt install golang-go

# Или через snap
sudo snap install go --classic
```

### Шаг 3: Установка Beads

**Вариант A: Из исходников (если есть репозиторий)**
```bash
git clone https://github.com/beads-dev/beads.git
cd beads
go build
sudo cp beads /usr/local/bin/
```

**Вариант B: Через пакетный менеджер (если доступен)**
```bash
# TBD - нужно найти официальный способ установки
```

**Вариант C: Pre-built binary (если доступен)**
```bash
# TBD - скачать с GitHub Releases
```

---

## 🔄 План интеграции

### Вариант 1: Замена `bd.sh` на Beads

**Плюсы:**
- ✅ Использование официального инструмента
- ✅ Больше функций и стабильность
- ✅ MCP интеграция

**Минусы:**
- ❌ Нужно мигрировать данные из `.beads/issues.jsonl`
- ❌ Нужно обновить все скрипты и алиасы
- ❌ Возможна несовместимость форматов

**Действия:**
1. Установить Beads
2. Проверить формат данных Beads
3. Создать скрипт миграции из `.beads/issues.jsonl` в формат Beads
4. Обновить `.aliases` для использования `beads` вместо `bd`
5. Обновить все скрипты, использующие `bd.sh`

### Вариант 2: Параллельное использование

**Плюсы:**
- ✅ Сохраняется текущая система
- ✅ Можно тестировать Beads параллельно
- ✅ Меньше рисков

**Минусы:**
- ❌ Дублирование данных
- ❌ Нужно синхронизировать два трекера

**Действия:**
1. Установить Beads
2. Использовать Beads для новых проектов/эпиков
3. Оставить `bd.sh` для текущих задач
4. Постепенно мигрировать

### Вариант 3: Обертка для совместимости

**Плюсы:**
- ✅ Полная обратная совместимость
- ✅ Можно использовать оба инструмента
- ✅ Плавная миграция

**Минусы:**
- ❌ Нужно поддерживать обертку
- ❌ Дополнительный слой абстракции

**Действия:**
1. Установить Beads
2. Создать обертку `bd` которая вызывает либо `bd.sh`, либо `beads`
3. Постепенно переводить команды на Beads

---

## 🎯 Рекомендуемый подход

### Фаза 1: Исследование (сейчас)

1. ✅ Найти официальный репозиторий Beads
2. ✅ Изучить формат данных
3. ✅ Проверить совместимость с нашим форматом
4. ✅ Изучить API и команды

### Фаза 2: Установка и тестирование

1. Установить Beads
2. Протестировать на тестовом проекте
3. Проверить интеграцию с MCP (если нужно)
4. Сравнить функциональность с `bd.sh`

### Фаза 3: Миграция (если решено)

1. Создать скрипт миграции данных
2. Обновить `.aliases`
3. Обновить документацию
4. Мигрировать существующие задачи

---

## 🔧 Технические детали

### Текущая интеграция `bd.sh`

**Используется в:**
- `.aliases` - алиасы команд
- `CHEATSHEET.md` - документация
- `примеры-мультиагентс-воркфлоу.md` - примеры
- `scripts/generate-status.sh` - генерация STATUS.md
- `/create-issues` команда в Cursor

**Формат данных:**
- `.beads/issues.jsonl` - JSON Lines формат
- `.beads/status.json` - статус проекта

### Что нужно проверить в Beads

1. **Формат данных:**
   - Какой формат использует Beads?
   - Совместим ли с JSONL?
   - Можно ли импортировать наши данные?

2. **Команды:**
   - Какие команды есть в Beads?
   - Совместимы ли с нашими (`ready`, `start`, `complete`, `show`)?
   - Нужно ли обновлять алиасы?

3. **Интеграция:**
   - Как интегрируется с MCP?
   - Как работает с агентами?
   - Нужны ли дополнительные настройки?

---

## 📝 Следующие шаги

1. **Найти репозиторий Beads:**
   ```bash
   # Поиск в интернете
   # Проверка GitHub
   # Проверка документации из статьи
   ```

2. **Установить Go (если нужно):**
   ```bash
   sudo apt install golang-go
   ```

3. **Установить Beads:**
   ```bash
   # После нахождения репозитория
   ```

4. **Протестировать:**
   ```bash
   beads --help
   beads version
   ```

5. **Сравнить с `bd.sh`:**
   - Функциональность
   - Формат данных
   - Команды
   - Интеграция

---

## ⚠️ Важные вопросы

1. **Совместимость форматов:**
   - Можно ли импортировать `.beads/issues.jsonl` в Beads?
   - Нужна ли миграция данных?

2. **Обратная совместимость:**
   - Будут ли работать существующие скрипты?
   - Нужно ли обновлять алиасы?

3. **Функциональность:**
   - Что есть в Beads, чего нет в `bd.sh`?
   - Что есть в `bd.sh`, чего нет в Beads?

4. **Интеграция:**
   - Как Beads интегрируется с Spec-Driven workflow?
   - Нужна ли настройка MCP?

---

## 🔗 Ресурсы

- [Статья на Habr](https://habr.com/ru/articles/983500/)
- GitHub репозиторий: **TBD** (нужно найти)
- Документация: **TBD**

---

**Статус:** Ожидание информации о репозитории и способе установки

