# 📊 План упрощения CHEATSHEET.md

**Дата:** 2026-01-06  
**Цель:** Упростить CHEATSHEET.md, убрав дублирование legacy сценариев

---

## 🔍 НАЙДЕННЫЕ ДУБЛИРОВАНИЯ

### 1. Дублирование коммитов

**Текущее состояние:**
- Строка 107: `./step.sh` упоминается как альтернатива `git commit`
- Строки 383-412: Полный раздел "LLM-OS СИСТЕМА" с `step.sh`, `monitor.sh`, `generate_report.sh`

**Проблема:**
- Два способа коммита: основной (`git commit`) и legacy (`./step.sh`)
- `step.sh` обновляет `WORKFLOW_STATE.md` (legacy LLM-OS), но основной workflow использует `STATUS.md`

**Решение:**
- Оставить `git commit` как основной способ
- `step.sh` переместить в раздел "LEGACY (только для LLM-OS scenarios)"
- Убрать упоминание `step.sh` из основного workflow

---

### 2. Дублирование создания фич

**Текущее состояние:**
- Строки 112-186: Spec-Driven workflow (`/idea`, `/specify`, `/plan`, `/tasks`, `/create-issues`)
- Строки 451-489: Legacy feature management (`feature_new.sh`, `feature_impact.sh`)

**Проблема:**
- Два способа создания фич: основной (Spec-Driven) и legacy (scenarios)
- Legacy скрипты используются только в `scenarios/FEATURE_DEVELOPMENT.yml`, не в основном workflow

**Решение:**
- Оставить Spec-Driven как основной workflow
- Legacy feature management переместить в раздел "LEGACY (только для scenarios)"
- Добавить примечание: "Для нового Spec-Driven workflow используйте раздел 'НОВАЯ ФИЧА'"

---

### 3. Дублирование мониторинга

**Текущее состояние:**
- Строки 9-16: `STATUS.md` - основной способ проверки статуса
- Строки 400-401: `monitor.sh` - legacy мониторинг через `WORKFLOW_STATE.md`

**Проблема:**
- Два способа мониторинга: основной (`STATUS.md`) и legacy (`monitor.sh`)

**Решение:**
- Оставить `STATUS.md` как основной способ
- `monitor.sh` переместить в раздел "LEGACY"

---

### 4. Избыточные разделы

**Текущее состояние:**
- Строки 383-412: "LLM-OS СИСТЕМА (Legacy, но используется)"
- Строки 451-489: "FEATURE MANAGEMENT (Используется в scenarios)"
- Строки 493-524: "SCENARIO MANAGEMENT (Используется в scenarios)"
- Строки 527-551: "CONTEXT COLLECTION (PLAN/BUILD workflow)"

**Проблема:**
- Все эти разделы относятся к legacy LLM-OS системе
- Они не используются в основном Spec-Driven + Issue Tracking workflow
- Создают путаницу и дублирование

**Решение:**
- Объединить все legacy разделы в один: "LEGACY LLM-OS (только для scenarios)"
- Добавить четкое примечание: "Эти скрипты используются только в legacy LLM-OS scenarios, не в основном workflow"

---

## 🎯 ПЛАН УПРОЩЕНИЯ

### Структура после упрощения:

1. **НАЧАЛО ДНЯ / СЕССИИ** (без изменений)
2. **ВО ВРЕМЯ РАБОТЫ** (без изменений)
3. **ЗАВЕРШЕНИЕ РАБОТЫ** (убрать `./step.sh`, оставить только `git commit`)
4. **НОВАЯ ФИЧА (Spec-Driven)** (без изменений)
5. **ПОИСК И ФИЛЬТРАЦИЯ** (без изменений)
6. **УПРАВЛЕНИЕ ЗАДАЧАМИ** (без изменений)
7. **ТИПИЧНЫЕ СЦЕНАРИИ** (без изменений)
8. **РЕШЕНИЕ ПРОБЛЕМ** (без изменений)
9. **БЫСТРЫЕ КОМАНДЫ** (без изменений)
10. **КЛЮЧЕВЫЕ ПРАВИЛА** (без изменений)
11. **DEPLOYMENT СКРИПТЫ** (оставить - используются)
12. **UTILITY СКРИПТЫ** (оставить - используются)
13. **LEGACY LLM-OS (только для scenarios)** (НОВЫЙ объединенный раздел)
   - LLM-OS система (step.sh, monitor.sh, setup_workflow.sh)
   - Feature Management (feature_new.sh, feature_impact.sh)
   - Scenario Management (quality_gate.sh, rollback_stage.sh)
   - Context Collection (collect/backend-auth.sh)
14. **ДОПОЛНИТЕЛЬНАЯ ДОКУМЕНТАЦИЯ** (без изменений)
15. **АРХИВНЫЕ СКРИПТЫ** (без изменений)

---

## ✅ ПРЕИМУЩЕСТВА УПРОЩЕНИЯ

1. **Четкость:** Основной workflow (Spec-Driven + Issue Tracking) выделен
2. **Нет путаницы:** Legacy скрипты четко отделены
3. **Меньше дублирования:** Один способ для каждой операции
4. **Проще навигация:** Меньше разделов, проще найти нужное
5. **Стабильность:** Основной workflow не затронут

---

## ⚠️ ВАЖНО

**НЕ удалять:**
- Legacy скрипты (они используются в scenarios)
- Deployment скрипты (используются)
- Utility скрипты (используются)

**Только реорганизовать:**
- Объединить legacy разделы
- Убрать дублирование из основного workflow
- Добавить четкие примечания о назначении

---

**Последнее обновление:** 2026-01-06

