# 🚨 КРИТИЧЕСКАЯ УЯЗВИМОСТЬ: РОТАЦИЯ GITHUB TOKEN ОБЯЗАТЕЛЬНА

**Дата:** 2025-12-26  
**Статус:** 🔴 **КРИТИЧНО** - Токен был закоммичен и отправлен на GitHub  
**Коммит:** `d41fa3f30c4db12ecff1dd0684e519efe4bf8535`  
**Ветка:** `main` (origin/main)

---

## 🔍 ЧТО ПРОИЗОШЛО

**Реальный GitHub Personal Access Token был закоммичен в Git и отправлен на GitHub.**

- **Токен:** `ghp_REDACTED`
- **Коммит:** `d41fa3f30c4db12ecff1dd0684e519efe4bf8535`
- **Файл:** `AGENTS_SETUP.md`
- **Ветка:** `main` (origin/main)
- **Статус:** **Скомпрометирован** - доступен в истории Git на GitHub

**⚠️ КРИТИЧНО:** Если репозиторий публичный или доступен другим пользователям, токен считается **скомпрометированным** и должен быть **немедленно отозван**.

---

## 🛡️ НЕМЕДЛЕННЫЕ ДЕЙСТВИЯ (ВЫПОЛНИТЬ СЕЙЧАС)

### 1. Отзовите токен (КРИТИЧНО - ВЫПОЛНИТЬ НЕМЕДЛЕННО)

**⏰ ВРЕМЯ:** Выполните в течение **5 минут** после обнаружения.

**Токен для отзыва:** `ghp_REDACTED`

#### Вариант A: Регенерация токена (РЕКОМЕНДУЕТСЯ)

1. Откройте: https://github.com/settings/tokens
2. Найдите токен `ghp_REDACTED`
3. Нажмите **Regenerate** (регенерировать)
4. **✅ Преимущества:**
   - Старый токен автоматически отзывается
   - Новый токен создается с тем же именем
   - Не нужно искать и удалять старый токен вручную
5. **⚠️ ВАЖНО:** Немедленно скопируйте новый токен (показывается только один раз!)

#### Вариант B: Отзыв и создание нового

1. Откройте: https://github.com/settings/tokens
2. Найдите токен `ghp_REDACTED`
3. Нажмите **Revoke** → подтвердите удаление
4. Создайте новый токен (см. Шаг 2)

---

### 2. Создайте новый токен (3 минуты)

#### Шаг 1: Генерация нового токена

1. Откройте: https://github.com/settings/tokens
2. Нажмите **Generate new token (classic)**
3. Настройки:
   - **Note:** `Flow Logic Multi-Agent System` (или другое описание)
   - **Expiration:** 90 дней (или меньше)
   - **Scopes:**
     - ✅ `repo` (Full control of private repositories)
     - ✅ `workflow` (Update GitHub Action workflows)
     - ✅ `read:org` (Read org and team membership, если нужно)
4. Нажмите **Generate token**
5. **⚠️ ВАЖНО:** Немедленно скопируйте токен (показывается только один раз!)

#### Шаг 2: Сохраните новый токен

**Локально (для разработки):**
```bash
# Добавить в .env (НЕ коммитить!)
echo "GITHUB_TOKEN=ghp_ваш_новый_токен" >> .env
```

**CI/CD (GitHub Actions):**
1. GitHub → Settings → Secrets and variables → Actions
2. Найдите `GITHUB_TOKEN` или `MY_GITHUB_TOKEN`
3. Нажмите **Update** → вставьте новый токен → **Update secret**

**Production (если используется):**
- AWS SSM Parameter Store: `/flowlogic/production/github/token`

---

### 3. Обновите все места использования

Проверьте, где используется токен:

1. **Локальные .env файлы** (у всех разработчиков)
2. **GitHub Secrets** (для CI/CD)
3. **AWS SSM Parameters** (если используется)
4. **Другие системы** (если есть)

---

### 4. Очистка истории Git (ОПЦИОНАЛЬНО, но рекомендуется)

**⚠️ ВАЖНО:** Токен уже в истории Git на GitHub. Даже после удаления из текущих файлов, он останется в истории.

#### Вариант A: Если репозиторий приватный и у вас есть доступ

Можно переписать историю Git (требует force push):

```bash
# ВНИМАНИЕ: Это перепишет историю Git!
# Убедитесь, что все участники проекта синхронизированы

# Использовать BFG Repo-Cleaner (рекомендуется)
# Или git filter-branch
git filter-branch --force --index-filter \
  "git rm --cached --ignore-unmatch AGENTS_SETUP.md" \
  --prune-empty --tag-name-filter cat -- --all

# После очистки - force push (ОСТОРОЖНО!)
# git push origin --force --all
```

#### Вариант B: Если репозиторий публичный или нельзя переписать историю

1. **Считайте токен скомпрометированным** (уже сделано выше)
2. Отзовите токен (см. Шаг 1)
3. Создайте новый токен (см. Шаг 2)
4. Мониторьте активность токена (если возможно)

---

## ✅ ЧЕКЛИСТ (ВЫПОЛНИТЬ В ТЕЧЕНИЕ 5 МИНУТ)

### Критичные действия (сейчас):
- [ ] **Токен отозван в GitHub** (`ghp_REDACTED`)
- [ ] **Новый токен создан**
- [ ] **Новый токен сохранен в .env** (локально)
- [ ] **Новый токен обновлен в GitHub Secrets** (для CI/CD)

### Проверка (после ротации):
- [ ] Все агенты работают с новым токеном
- [ ] GitHub Actions workflows работают
- [ ] История Git проверена (уже выполнено - токен найден)

### Долгосрочные меры:
- [ ] Рассмотрена очистка истории Git
- [ ] Настроены pre-commit hooks для проверки секретов
- [ ] Обновлена документация по безопасности

---

## 🔍 МОНИТОРИНГ

После ротации токена:

1. **Проверьте активность старого токена:**
   - GitHub → Settings → Security → Personal access tokens
   - Проверьте, что старый токен отозван
   - Проверьте последнюю активность (должна быть до отзыва)

2. **Проверьте активность нового токена:**
   - Убедитесь, что новый токен работает
   - Проверьте, что все системы используют новый токен

3. **Проверьте репозиторий:**
   - Убедитесь, что нет других мест с токеном
   - Проверьте историю Git на наличие других токенов

---

## 📋 ПРОВЕРКА ИСТОРИИ GIT

```bash
# Уже выполнено - токен найден в коммите:
# d41fa3f30c4db12ecff1dd0684e519efe4bf8535
git log --all -p -S "ghp_REDACTED"
```

---

## 🔗 СВЯЗАННЫЕ ДОКУМЕНТЫ

- `docs/security/github_token_security.md` - Общая документация по безопасности токенов
- `docs/security/secret_rotation_required.md` - Ротация AWS Access Keys

---

**ВАЖНО:** Ротация токена должна быть выполнена как можно скорее!

**Последнее обновление:** 2025-12-26

