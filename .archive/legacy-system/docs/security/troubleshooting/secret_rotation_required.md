# 🚨 КРИТИЧЕСКАЯ УЯЗВИМОСТЬ: РОТАЦИЯ СЕКРЕТОВ ОБЯЗАТЕЛЬНА

**Дата:** 2025-12-26  
**Статус:** 🔴 **КРИТИЧНО** - Секрет был закоммичен и отправлен на GitHub  
**Коммит:** `ff2c747c03c771c488f70dadb846959c99fe3319`  
**Ветка:** `main` (origin/main)

---

## 🔍 ЧТО ПРОИЗОШЛО

**Реальный Access Key ID был закоммичен в Git и отправлен на GitHub.**

- Access Key ID: `AKIA_REDACTED`
- Коммит: `ff2c747c03c771c488f70dadb846959c99fe3319`
- Ветка: `main` (origin/main)
- Статус: **Скомпрометирован** - доступен в истории Git на GitHub

**⚠️ КРИТИЧНО:** Если репозиторий публичный или доступен другим пользователям, Access Key считается **скомпрометированным** и должен быть **немедленно удален**.

---

## 🛡️ НЕМЕДЛЕННЫЕ ДЕЙСТВИЯ (ВЫПОЛНИТЬ СЕЙЧАС)

### 1. Ротация Access Keys (КРИТИЧНО - ВЫПОЛНИТЬ НЕМЕДЛЕННО)

**⏰ ВРЕМЯ:** Выполните в течение **15 минут** после обнаружения.

**Access Key ID для ротации:** `AKIA_REDACTED`

#### Шаг 1: Создайте новый Access Key

1. AWS Console → IAM → Users → `flowlogic-cicd-user`
2. **Security credentials** tab
3. **Create access key** → **Application running outside AWS**
4. **Скопируйте** новый Access Key ID и Secret Access Key

#### Шаг 2: Обновите GitHub Secrets (2 минуты)

1. Откройте: `https://github.com/recreomassage-hub/flowlogic.shop/settings/secrets/actions`
2. Найдите секрет `AWS_ACCESS_KEY_ID`
3. Нажмите **Update** → вставьте новый Access Key ID → **Update secret**
4. Найдите секрет `AWS_SECRET_ACCESS_KEY`
5. Нажмите **Update** → вставьте новый Secret Access Key → **Update secret**

#### Шаг 3: Удалите старый Access Key (1 минута)

**⚠️ КРИТИЧНО:** Удалите старый ключ **немедленно** после обновления GitHub Secrets.

1. AWS Console → IAM → Users → `flowlogic-cicd-user`
2. **Security credentials** tab
3. Найдите Access Key ID: `AKIA_REDACTED`
4. Нажмите **Delete** → подтвердите удаление
5. **Не ждите** проверки нового ключа - старый уже скомпрометирован!

---

### 2. Очистка истории Git (ОПЦИОНАЛЬНО, но рекомендуется)

**⚠️ ВАЖНО:** Секрет уже в истории Git на GitHub. Даже после удаления из текущих файлов, он останется в истории.

#### Вариант A: Если репозиторий приватный и у вас есть доступ

Можно переписать историю Git (требует force push):

```bash
# ВНИМАНИЕ: Это перепишет историю Git!
# Убедитесь, что все участники проекта синхронизированы

# Использовать BFG Repo-Cleaner (рекомендуется)
# Или git filter-branch
git filter-branch --force --index-filter \
  "git rm --cached --ignore-unmatch docs/security/secret_rotation_required.md" \
  --prune-empty --tag-name-filter cat -- --all

# После очистки - force push (ОСТОРОЖНО!)
# git push origin --force --all
```

#### Вариант B: Если репозиторий публичный или нельзя переписать историю

1. **Считайте Access Key скомпрометированным** (уже сделано выше)
2. Ротируйте ключи (см. Шаг 1)
3. Мониторьте CloudTrail на подозрительную активность
4. Рассмотрите создание нового репозитория (если возможно)

#### Проверка истории:

```bash
# Уже выполнено - секрет найден в коммите:
# ff2c747c03c771c488f70dadb846959c99fe3319
git log --all -p -S "AKIA_REDACTED"
```

---

### 3. Мониторинг и проверка (после ротации)

#### Немедленно после ротации:

1. **Проверьте CloudTrail** на подозрительную активность:
   - AWS Console → CloudTrail → Event history
   - Фильтр: User name = `flowlogic-cicd-user`
   - Проверьте последние 24 часа на неавторизованные действия

2. **Проверьте использование старого Access Key:**
   - Должно быть 0 использований после удаления
   - Если есть активность после удаления - **немедленно** проверьте безопасность

3. **Протестируйте новый Access Key:**
   - Запустите GitHub Actions workflow
   - Убедитесь, что деплой работает с новыми ключами

#### Долгосрочный мониторинг:

- Настройте CloudWatch Alarms для подозрительной активности
- Регулярно проверяйте список Access Keys в IAM
- Рассмотрите переход на OIDC (более безопасный метод)

---

## ✅ ЧЕКЛИСТ (ВЫПОЛНИТЬ В ТЕЧЕНИЕ 15 МИНУТ)

### Критичные действия (сейчас):
- [ ] **Новый Access Key создан в AWS**
- [ ] **GitHub Secrets обновлены** (AWS_ACCESS_KEY_ID и AWS_SECRET_ACCESS_KEY)
- [ ] **Старый Access Key удален из AWS** (`AKIA_REDACTED`)
- [ ] **CloudTrail проверен** на подозрительную активность

### Проверка (после ротации):
- [ ] GitHub Actions workflow протестирован с новыми ключами
- [ ] Деплой успешно выполнен
- [ ] Использование старого ключа = 0 (проверено в CloudTrail)
- [ ] История Git проверена (уже выполнено - секрет найден)

### Долгосрочные меры:
- [ ] Рассмотрен переход на OIDC (более безопасный метод)
- [ ] Настроены CloudWatch Alarms для мониторинга
- [ ] Обновлена документация по безопасности

---

## 🔗 СВЯЗАННЫЕ ДОКУМЕНТЫ

- `docs/deployment/aws_credentials_setup.md` - Настройка Access Keys
- `docs/security/github_token_security.md` - Безопасность GitHub токенов

---

**ВАЖНО:** Ротация секретов должна быть выполнена как можно скорее!


