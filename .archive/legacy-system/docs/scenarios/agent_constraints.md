# 🔒 Ограничения агентов в сценарном режиме

**Дата создания:** 2025-12-26  
**Цель:** Четкие правила для агентов в автоматическом режиме

---

## ⚠️ КРИТИЧЕСКИЕ ПРАВИЛА

### ❌ АГЕНТ НЕ МОЖЕТ:

1. **Менять сценарий** (`current_scenario` в `SCENARIO_STATE.yml`)
   - Только системные события могут менять сценарий
   - Агент не может решать, какой сценарий использовать

2. **Менять фазу** (`phase` в `SCENARIO_STATE.yml`)
   - Только система может переходить между фазами
   - Агент не может "перепрыгнуть" фазы

3. **Запускать скрипты напрямую**
   - Агент не может вызывать `./scripts/*.sh` напрямую
   - Только через `allowed_actions`

4. **Импровизировать процессные решения**
   - Агент не может "придумать" новый процесс
   - Агент следует только `allowed_actions`

5. **Менять `SCENARIO_STATE.yml`**
   - Файл изменяется только системой
   - Агент может только читать

---

### ✅ АГЕНТ МОЖЕТ:

1. **Выполнять `allowed_actions`**
   - Список действий определяется сценарием и фазой
   - Агент может выполнять только эти действия

2. **Обновлять артефакты**
   - Код (в разрешенных директориях)
   - Документация
   - Тесты

3. **Фиксировать результат**
   - Коммиты через `step.sh` (если разрешено)
   - Обновление `WORKFLOW_STATE.md`

4. **Читать конфигурацию**
   - `PROJECT_CONFIG.md`
   - `SCENARIO_STATE.yml` (только чтение)
   - `WORKFLOW_STATE.md`

---

## 📋 ПРИМЕРЫ РАЗРЕШЕННЫХ ДЕЙСТВИЙ

### FEATURE_DEVELOPMENT:IMPLEMENTATION
```yaml
allowed_actions:
  - update_code
  - commit_changes
  - run_tests
```

**Агент может:**
- ✅ Изменять код в `src/`
- ✅ Вызывать `./step.sh` для коммита
- ✅ Запускать тесты

**Агент НЕ может:**
- ❌ Менять сценарий на DEPLOYMENT
- ❌ Пропустить фазу QA
- ❌ Запускать `./scripts/deploy.sh` напрямую

---

### INCIDENT_RECOVERY:FREEZE
```yaml
allowed_actions:
  - freeze_deployments
  - create_backup
```

**Агент может:**
- ✅ Создавать backup
- ✅ Фиксировать текущее состояние

**Агент НЕ может:**
- ❌ Пытаться "исправить" проблему
- ❌ Пропустить ROLLBACK
- ❌ Импровизировать решение

---

## 🎯 КАК ЭТО РАБОТАЕТ

### Для агента в Cursor:

**Правильно:**
```
ROLE: BACKEND_DEV
Сценарий: FEATURE_DEVELOPMENT
Фаза: IMPLEMENTATION
Разрешенные действия: update_code, commit_changes

Задача: Реализовать endpoint /api/v1/tasks/prioritize
```

**Неправильно:**
```
ROLE: BACKEND_DEV
Задача: Реализовать endpoint и сразу задеплоить
(❌ Агент не может решать о деплое)
```

---

## 🔄 ПЕРЕХОДЫ МЕЖДУ СЦЕНАРИЯМИ

### Автоматические (системные события):
- Deploy failure → `INCIDENT_RECOVERY`
- Quality Gate FAIL → `ROLLBACK`
- Feature completed → `DEPLOYMENT`

### Ручные (только OWNER):
```bash
./llmos scenario set FEATURE_DEVELOPMENT BRIEF
```

**Агент НЕ может делать это сам.**

---

## 📚 ДОПОЛНИТЕЛЬНЫЕ РЕСУРСЫ

- **Справочник сценариев:** `scenarios_reference.md`
- **SCENARIO_STATE.yml** - Текущее состояние
- **scenario_engine.sh** - Движок сценариев

---

**Последнее обновление:** 2025-12-26



