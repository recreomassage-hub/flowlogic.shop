# Анализ статьи о Beads и сравнение с нашей реализацией

**Источник:** [Лучшие практики Beads - Habr](https://habr.com/ru/articles/983500/)  
**Дата анализа:** 2026-01-09

---

## 📊 Проверка установки Beads

**Результат:** ❌ **Beads НЕ установлен в вашей системе**

```bash
$ which beads
# Команда не найдена

$ beads --version
# beads: command not found
```

**Вывод:** В вашей системе используется **собственная реализация** `bd.sh`, а не официальный Beads.

---

## 🔍 Ключевые моменты из статьи

### Что такое Beads (официальный)

1. **Инструмент исполнения, а не планирования**
   - Beads - это инструмент для **отслеживания задач** (tracking)
   - Не планирует, а **управляет выполнением**
   - Написан на **Go** (130K строк кода, ~50% тесты)

2. **Философия: "Agents on Rails"**
   - Фокус на отслеживании, а не на планировании
   - Работает как "рабочая память" между сессиями агентов
   - Позволяет агентам работать параллельно над разными задачами

3. **Интеграция с планированием**
   - Планирование делается **вне** Beads (например, OpenSpec)
   - Затем план импортируется в Beads как эпики и задачи
   - Beads отслеживает выполнение

4. **Лучшие практики из статьи:**
   - ✅ Планируйте вне Beads, затем импортируйте
   - ✅ Часто перезапускайте агентов (одна задача на сессию)
   - ✅ Просите ИИ создавать много задач (для работы >2 минут)
   - ✅ Используйте короткие префиксы задач (`bd-`, `vc-`, `wy-`)
   - ✅ Beads действует как рабочая память между сессиями

---

## 🔄 Сравнение: Официальный Beads vs Наш `bd.sh`

### Официальный Beads

| Характеристика | Описание |
|---------------|----------|
| **Язык** | Go (130K строк) |
| **Установка** | Отдельный бинарный файл |
| **Формат данных** | Собственный формат |
| **Интерфейс** | CLI + API |
| **Интеграция** | MCP, Agent Mail |
| **Статус** | Активная разработка, вирусный рост |

### Наш `bd.sh` (Beads CLI)

| Характеристика | Описание |
|---------------|----------|
| **Язык** | Bash + jq |
| **Установка** | Скрипт в проекте (`scripts/bd.sh`) |
| **Формат данных** | JSONL (`.beads/issues.jsonl`) |
| **Интерфейс** | CLI только |
| **Интеграция** | Специфична для нашего проекта |
| **Статус** | Локальная реализация для проекта |

---

## ✅ Что у нас реализовано (соответствует философии Beads)

### 1. Инструмент исполнения, не планирования
- ✅ Наш `bd.sh` - это трекер задач
- ✅ Планирование делается через Spec-Driven (`/specify`, `/plan`, `/tasks`)
- ✅ Затем задачи импортируются через `/create-issues`

### 2. Рабочая память между сессиями
- ✅ `.beads/issues.jsonl` хранит все задачи
- ✅ `STATUS.md` - человеко-читаемый обзор
- ✅ Агенты могут работать параллельно над разными задачами

### 3. Управление зависимостями (граф задач)
- ✅ Поле `blocked_by` для зависимостей
- ✅ Команда `bd ready` проверяет статус блокирующих задач
- ✅ Задачи показываются как готовые, когда все зависимости завершены

### 4. Короткие префиксы
- ✅ Поддержка префиксов при создании задач (`DS-`, `DASH-`, и т.д.)
- ✅ Можно задавать при `/create-issues`

---

## ❌ Что у нас НЕ реализовано (из официального Beads)

### 1. MCP интеграция
- ❌ Нет интеграции с MCP (Model Context Protocol)
- ❌ Нет Agent Mail для обмена сообщениями между агентами

### 2. Масштабируемость
- ❌ Наш скрипт работает только локально
- ❌ Нет распределенной работы между несколькими репозиториями

### 3. UI
- ❌ Нет пользовательского интерфейса (но и в Beads его нет - только примеры)

### 4. Автоматическая оркестрация
- ❌ Нет встроенной оркестрации (но и в Beads её нет - это отдельный слой)

---

## 🎯 Рекомендации на основе статьи

### 1. Следовать практикам Beads

**✅ Уже делаем:**
- Планируем вне системы (Spec-Driven), затем импортируем
- Используем короткие префиксы
- Создаем задачи для работы >2 минут
- Часто перезапускаем агентов (одна задача на сессию)

**💡 Можно улучшить:**
- Добавить автоматическое создание задач при код-ревью
- Улучшить итерацию при создании эпиков (до 5 раз)
- Добавить проверку качества перед началом работы

### 2. Интеграция с MCP Agent Mail

**Возможность:** Интегрировать наш `bd.sh` с MCP Agent Mail для:
- Обмена сообщениями между агентами
- Координации работы над задачами
- Автоматического распределения задач

### 3. Улучшение системы зависимостей

**Уже исправлено:**
- ✅ Команда `bd ready` теперь проверяет статус блокирующих задач
- ✅ Граф задач работает корректно

---

## 📝 Выводы

### Текущая ситуация

1. **Официальный Beads НЕ установлен** - используется собственная реализация
2. **Наш `bd.sh` следует философии Beads:**
   - Инструмент исполнения, не планирования
   - Рабочая память между сессиями
   - Управление зависимостями
   - Интеграция с Spec-Driven планированием

3. **Различия:**
   - Наш скрипт проще (Bash vs Go)
   - Локальный, не распределенный
   - Нет MCP интеграции
   - Но функционально очень похож

### Рекомендации

1. **Продолжать использовать наш `bd.sh`** - он хорошо работает для проекта
2. **Следовать практикам из статьи:**
   - Одна задача на сессию агента
   - Частые перезапуски агентов
   - Создавать задачи для работы >2 минут
   - Итерировать эпики до 5 раз перед началом работы

3. **Рассмотреть интеграцию с MCP** (если нужна координация между агентами)

4. **Не устанавливать официальный Beads** (если текущая система работает)

---

## 🔗 Ссылки

- [Оригинальная статья на Habr](https://habr.com/ru/articles/983500/)
- [GitHub Beads](https://github.com/beads-dev/beads) (предположительно)
- [MCP Agent Mail](https://github.com/jeffreyemanuel/mcp-agent-mail) (упоминается в статье)

---

**Вывод:** Ваша система уже следует лучшим практикам Beads, но использует собственную реализацию вместо официального инструмента. Это нормально и работает для вашего проекта.

